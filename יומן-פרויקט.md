# 📓 יומן הפרויקט — CrossFit Bat Yam Capacity

> קובץ אחד שמסכם מה נעשה, מה הוחלט ולמה, ומה פתוח. מתעדכן בכל סשן עבודה.
> (מתוחזק ע"י Claude — אם עשינו משהו ולא רשום כאן, תגיד לי ואוסיף.)

---

## מה זה הפרויקט
אפליקציית מעקב לתוכנית **Capacity** (8 שבועות) של הבוקס — יומן אימונים אישי, דירוג תחרותי, שיאים, וניהול משתתפים.
- **כתובת:** crossfit-bat-yam-capacity.vercel.app (כל push לגיטהאב = עדכון אוטומטי)
- **מסד נתונים:** Supabase (פרויקט "Crossfit BatYam Capacity") — התחברות, סנכרון, דירוג משותף
- **אדמין:** משתמש `Oriuziel9300` · כניסת מנהל באפליקציה: לחיצה על הלוגו + קוד `batyam`

---

## מה נבנה, לפי סדר

### תשתית והתחברות (16-17/07)
- תוקן באג RLS קריטי (מדיניות רקורסיבית) שמנע יצירת פרופילים — בגללו משתמשים לא נשמרו.
- אופס המסד לגמרי לקראת הפיילוט; נוצר חשבון האדמין Oriuziel9300.
- פאנל "👥 משתתפים" מופיע **רק במצב אדמין** (לוגו → קוד), ונעלם מיד ביציאה.

### העיצוב החדש (17/07)
- הוחלף כל ממשק האפליקציה לעיצוב החדש מ-Claude Design (סקירה, יומן, דירוג, שיאים + תוכן האימונים).
- שכבת ההתחברות/סנכרון נשמרה. תוקנו 3 באגים בעיצוב החדש (הקשה ביותר: הדירוג לא נטען בכלל).
- מסך טעינה ממותג עם לוגו הבוקס + ביטול כל החלוניות שקופצות אוטומטית (מדריך רק בלחיצה).

### דירוג ונוכחות (18/07)
- כל השלמת אימון מסתנכרנת ללוח אוטומטית (קודם — רק מי שהזין תוצאה ידנית הופיע).
- הדירוג מציג את **כל** הרשומים מרגע ההרשמה, גם עם 0 אימונים.
- האדמין משתתף בדירוג כמו כולם (בוטלה ההסתרה).
- הוסר עורך המתחרים הידני מהדירוג — הכל אוטומטי.

### קטגוריות תחרות (18-19/07)
- בכניסה ראשונה כל מתאמן מזין **מין + תאריך לידה**.
- 6 קטגוריות: TEEN (עד 18) / ELITE (34-18) / MASTERS (35+) × גברים/נשים.
- סינון בדירוג לפי קטגוריה — **דירוג נפרד** בתוך כל קטגוריה.
- **כרטיס פרופיל** (כפתור 👤): שם, קטגוריה, גיל, סה"כ אימונים, מיקום בקטגוריה, 3 שיאים אחרונים + **תגי הישגים** (6 תגים).

### חוויית שימוש (19/07)
- **PWA** — האפליקציה ניתנת להתקנה על מסך הבית בטלפון (+הסבר במדריך).
- **דירוג בזמן אמת** — הלוח מתעדכן לבד בלי רענון.
- **מחיקת משתמש אמיתית** — מהפאנל, מוחקת גם את חשבון ההתחברות.
- המדריך נדחס למסך אחד; שיתוף אימון בודד כתמונה (הוסר שיתוף השיאים).

### מנוע הניקוד — "כמו ב-Open" (19-23/07)
ההחלטות שסגרנו:
- **רק המטקון נספר** בדירוג (לא LIFT ולא EXTRA).
- **רק ביצועי RX** — צ'קבוקס "עשיתי RX" ליד כל מטקון (כבוי כברירת מחדל). מי שהקטין משקל: נספר לנוכחות בלבד, לא לתוצאה.
- כל מטקון RX מדורג מול כל מי שעשה אותו RX → **אחוזון** (ראשון = 0). זמן=מהיר מנצח; חזרות/כמות=יותר מנצח.
- **ניקוד שבועי = 70% ממוצע האחוזונים + 30% נוכחות** (החמצות מהיעד). פחות = טוב יותר.
- **אין הזנה ידנית** — הקובייה בדירוג קריאה-בלבד.
- למה אחוזון ולא "מקום"? כדי שהדירוג יהיה הוגן גם כשמספר המשתתפים שונה בין אימונים.
- למה ממוצע ולא סכום? כדי שמי שמתאמן יותר לא ייענש; הכמות נספרת דרך רכיב הנוכחות.

### אימוני שבוע 2 (23/07)
- הוטמעו כל 7 ימי האימון של שבוע 2 מתוך צילומי התוכנית (PROGRAM_VERSION 6→7 — מתעדכן אוטומטית אצל כולם בלי לפגוע בתוצאות שנרשמו).
- ראשון: Clean & Jerk קומפלקס + AMRAP 12:00 בזוגות · שני: Halting Snatch+OHS + Aerobic Session (סינדי) · שלישי הגדול: דדליפטים + **שני מטקונים עם שני ציונים נפרדים** (שאטלים / EMOMania) · רביעי: Back Squats + מטקון KB רגליים · חמישי: מתח EMOM (המשך CapaciTest #4) + Every 4:00 · שישי: Power Snatch + אינטרוולים חתירה/ריצה · שבת: כוח פלג גוף עליון + AMRAP 20:00 בזוגות.
- החלטות ניקוד (עם אורי): יום שני נמדד ב**זמן עבודה כולל**; "KB side" החתוך בצילום = KB side bends.
- `applyProgram` הורחב לתמוך בכמה שבועות (עד עכשיו ידע למלא רק את שבוע 1).
- נוסף סשן אלטרנטיבי (אירובי) ליום ראשון, כמו בשבוע 1: AMRAP 16:00 (ריצה/KBS/BBJO/חתירה) + METCON-B בסגנון E6MOM 18:00 (ריצה 800 + מקס קלוריות חתירה). PROGRAM_VERSION 7→8.

### תיקוני מובייל — "tight ויציב" (24/07)
לקראת שליחה לבעלים של הבוקס. הבעיה מצילום המסך של אורי: בטלפון הכל נראה מוקטן, נמתח ונחתך.
- **שורש הבעיה:** שורת הכפתורים ב-header (פרופיל/מדריך/טאבים) הייתה קשיחה ברוחב 450px — ניפחה את כל הדף ל-470px על מסך 375px, מה שגרם לזום-אאוט וגלילה אופקית בכל האפליקציה.
- באפליקציה אין אף media query (הכל inline styles) — נוסף בלוק CSS מובייל ב-`app.html` עם `!important` שמנצח את ה-inline: header דו-שורתי בטלפון, טאבים ברוחב מלא, `overflow-x:hidden` גלובלי.
- **ביטול הזום האוטומטי של iOS:** כל האינפוטים 16px+ במובייל (מתחת לזה ספארי עושה זום פנימה בכל פוקוס) — גם ב-`index.html`.
- טבלת הדירוג: עמודות קבועות צרות יותר בטלפון כדי שהשם יקבל מקום; הוסרה כפילות "(אתה)" בטקסט + תג "אתה".
- גרידים של minmax(300px) קורסים לעמודה אחת מתחת ל-400px.
- הטאב "🏆 שיאים אישיים" קוצר ל"🏆 שיאים" (נשבר לשתי שורות).
- **מצב dev חדש:** `app.html?dev=1` על localhost בלבד — מריץ את האפליקציה עם נתוני דמו בלי Supabase, לבדיקות UI מהירות (boot.js). + `.claude/launch.json` לשרת מקומי (python, פורט 8123).
- אומת בדפדפן ברוחבים 320/375/430: אפס גלישות בכל הטאבים והמודאלים.

### תיקון "האימון שלי נעלם" + ניקוד שבועות ריקים + שבוע 3 (24/07)
**הבאג:** אורי דיווח שאימון שהוזן לא נשמר גם אחרי סגירה ופתיחה. בבדיקה מקומית האפליקציה עצמה שומרת מצוין — הבעיה בסנכרון:
- הדחיפה ל-Supabase מחכה 800ms; סגירת האפליקציה בטלפון (או מעבר לאפליקציה אחרת) הורגת אותה לפני שיצאה, וטוקן שפג אחרי רקע ארוך מפיל אותה בשקט.
- ובפתיחה הבאה boot.js דרס את מה שנשמר במכשיר עם העותק הישן מהשרת → האימון "נעלם".

**התיקון (boot.js):**
- חותמת "לא-סונכרן" (`cfby_dirty_v1`) על כל שינוי ביומן; נמחקת רק אחרי שהשרת אישר את הכתיבה.
- בעליית האפליקציה: אם במכשיר יש שינויים שלא סונכרנו וחדשים מהשרת — המקומי מנצח ונדחף לשרת (במקום להידרס).
- דחיפה מיידית ברגע שהאפליקציה נסגרת/עוברת לרקע (בלי לחכות ל-800ms) + דחיפה חוזרת כשהאינטרנט חוזר.
- כשל דחיפה: רענון טוקן וניסיון שני, ואם עדיין נכשל — ניסיון חוזר כל 10 שניות + שגיאה בקונסול (עד עכשיו נבלע בשקט).
- אם לחשבון אין נתונים בשרת (משתמש חדש / נמחק ע"י אדמין) — המכשיר מנוקה, כדי שנתונים מחוקים לא "יקומו לתחייה".

**ניקוד:** שבוע שאף אחד עוד לא הזין בו כלום = 0 נקודות (קודם כל שבוע עתידי הוסיף לכולם 30 נק' קנס נוכחות, והעונה התחילה על 240). הממוצע בטבלת העונה מחולק רק בשבועות שהתחילו.

**אימוני שבוע 3** הוטמעו מצילומי התוכנית (PROGRAM_VERSION 8→9):
- ראשון: Power Clean+Hang Squat Clean + 6RFT (ריצה/ברפיז/פרונט סקווט) · שני: קומפלקס קלין + Mono Session (המשך CapaciTest #2) + Blurred Strength · שלישי: פרונט סקווט + Hello Paincave (צ'יפר, cap 14:00) · רביעי: "Big Tuesday on a Wednesday" — DB snatches + שני חלקים/שני ציונים (DU+HSPU / AMRAP 15) · חמישי: Power Snatch EMOM + מתח "Next Level" (Every 45s x 8) + EMOM אירובי · שישי: סנאץ' מהרצפה + 3x AMRAP 4:00 + בק סקווט כבד (Extra) · שבת: DB Bench+Rows + זוגות ריצה/קלוריות.
- שיבוץ הימים נקבע לפי תבנית השבועות הקודמים (הצילומים לא הגיעו לפי סדר): יום הצ'יפר עבר לשלישי כי ה-Big Tuesday עבר לרביעי (כמו שכתוב בבריף שלו).

### סבב פידבק 2 + איפוס מלא (25/07)
**האבחנה החשובה:** אורי דיווח "כלום לא השתנה וההזנה עדיין לא נשמרת" — בדיקה מול המסד הראתה שהשורה שלו עודכנה לאחרונה ב-23/07, כלומר **הטלפון שלו עדיין הריץ את הגרסה הישנה** (ה-PWA נשאר פתוח בזיכרון ולא משך את הקוד החדש). הפריסה עצמה חיה ותקינה. הפתרון: לסגור את האפליקציה לגמרי (החלקה למעלה) ולפתוח מחדש.
- **איפוס מלא של המסד** לבקשתו: נמחקו כל שורות states + board (נתוני האימונים של כולם). פרופילים, חשבונות והתוכנית המשותפת נשמרו. בוצע דרך ה-Management API מהדפדפן (הדשבורד עצמו לא נטען).
- **נעילת ימים עתידיים:** אפשר לצפות בתוכנית של כל יום עתידי, אבל כל שדות ההזנה (תוצאות, מאמץ, סיכום, מנוחה, שמירה) נעולים עם 🔒 "הזנת התוצאות תיפתח ביום האימון". נאכף גם בקוד (saveDay/toggleDone/toggleRest) וגם ב-UI.
- **סרגל השמירה בעברית נקייה:** "משקל לתרגיל הכוח · תוצאה למטקון · תוצאה למטקון השני" במקום ערבוב LIFT/B לטיני ששיבש את סדר המילים ב-RTL, + dir=rtl מפורש.
- **Header קומפקטי:** כפתור הפרופיל = 👤 בלבד (הוסבר במדריך), הטאבים נמתחים לכל רוחב השורה במובייל בלי גלילה צידית (אומת: overflow=0 ב-375px).
- **Safe-area לאייפון:** ה-header יורד מתחת לשורת הסטטוס (שעה/סוללה) עם `env(safe-area-inset-top)`; הרקע הכחול ממשיך מאחוריה.
- **מפת התוכנית:** הוסר התג "מתוכנן" מהמקרא — צבע כחול חלול = אימון זמין לצפייה, בלי הסבר.

### מחיקת משתמשים, נעילת אינדורנס-או-WOD, ניקוד AMRAP (25/07)
**מחיקת משתמשים באדמין נשברה:** "Could not find the function public.admin_delete_user(target) in the schema cache" — למרות שביומן סומן שהפונקציה הורצה, בפועל היא לא קיימת במסד (או קיימת בחתימה ישנה). ניסיון לתקן דרך הדשבורד מהדפדפן נכשל (הדשבורד שוב עלה לבן, וגישה לטוקן נחסמה) — **אורי צריך להריץ את בלוק ה-SQL בעצמו** (ראו "פתוח" למטה). בנוסף:
- `schema.sql` עכשיו עושה `drop function if exists` לפני היצירה + `notify pgrst, 'reload schema'` בסוף — הרצה חוזרת של הקובץ תמיד מרפאת את ה-RPC, כולל רענון הקאש של PostgREST.
- boot.js: אם ה-RPC חסר, הודעת השגיאה באדמין אומרת במפורש להריץ את schema.sql (במקום שגיאת cache קריפטית).

**נעילה הדדית ביום אינדורנס (ראשון):** ביום עם שני סשנים (WOD/אינדורנס) נספר ומוזן **אימון אחד בלבד**:
- ברגע שסשן אחד נשמר כהושלם, הסשן השני ננעל: כל שדות ההזנה מוחלפים ב-🔒 "השלמת כבר את…", וסרגל השמירה מסביר איך להתחרט (לבטל שמירה בסשן השני).
- נאכף גם בקוד (saveDay / toggleDone) ולא רק ב-UI; טאב הסשן שהושלם מקבל ✓.
- נתונים ישנים שבהם שני הסשנים כבר סומנו לא נפגעים — הנעילה חלה רק מהזנה חדשה והלאה.

**מדידת AMRAP בסיבובים וחזרות (החלטת אורי — כלל קבוע מעכשיו):** אימון AMRAP עם זמן קצוב נמדד ב-rounds + reps:
- **שישי W2 (Row/Run + BBJO):** שונה מ"זמן כולל" ל-**סיבובים + חזרות בתוך time cap 18:00** (בקשה מפורשת של אורי). מי שכבר הזין זמן אתמול — היום שלו נשאר "הושלם", רק צריך להזין מחדש סיבובים כדי להופיע בדירוג.
- **שלישי הגדול W3 (AMRAP 15:00):** שונה מ-total reps ל-rounds+reps (עוד לא הגיע — W3 מתחיל 26/07).
- PROGRAM_VERSION 9→10 — מתעדכן אצל כולם אוטומטית.
- **הגנה חדשה:** תוצאה שהוזנה תחת שיטת מדידה קודמת עדיין נחשבת "הוזנה" (`metconLogged`) — שינוי שיטת מדידה בדיעבד לא מבטל בשקט ימים שכבר נשמרו.
- **לא שונו בכוונה:** AMRAP-ים של W1 (שבוע סגור — שינוי שיטה בדיעבד היה מערבב זמנים/חזרות/סיבובים בדירוג), וה-AMRAP הזוגי של ראשון W2 (הציון שם הוא סכום קלוריות+חזרות קבוצתי — אין "סיבובים"). מעכשיו כל AMRAP חדש ייכתב עם `resultMode:'rounds'`.

### סנכרון "תמיד אונליין" + חיווי גלוי (25/07, ערב)
**הרקע:** אורי שאל למה בכל תיקון התוצאות שלו נמחקות חוץ מהאימון הראשון. האבחנה: הדחיפות לענן נכשלו בשקט בטלפון (טיימר הדיבאונס נהרג בסגירת האפליקציה / טוקן שפג ברקע), הענן נשאר תקוע על אימון 1, וכל דיפלוי גרם לטעינה-מחדש שבה העותק הישן מהענן דרס את הטלפון. תיקון ה-dirty-stamp מ-24/07 סוגר את זה — אבל הוחלט להוסיף עוד שכבות כך שהמצב יהיה "תמיד אונליין, ובמקרה הגרוע הגרסה האחרונה שנשמרה":
- **חיווי סנכרון גלוי** (גלולה קטנה בפינה השמאלית-תחתונה): "⏳ שומר…" בזמן דחיפה, "✓ נשמר בענן" ירוק שנעלם אחרי 1.6 שניות, "⚠️ לא מסונכרן — מנסה שוב" אדום קבוע בכשל, "📡 אין אינטרנט — יסונכרן כשיחזור" בניתוק. אין יותר כשל שקט — המשתמש רואה מיד אם התוצאה הגיעה לענן.
- **דחיפת keepalive ביציאה:** ברגע שהאפליקציה עוברת לרקע/נסגרת, הדחיפה עוברת ישירות ל-REST של Supabase עם fetch keepalive — הדפדפן משלים את הבקשה גם אחרי שהעמוד נהרג. (supabase-js לא תומך בזה, אז זו קריאה ישירה עם הטוקן שנשמר בזיכרון.)
- **דופק כל 15 שניות:** כל עוד יש נתונים לא-מסונכרנים והאפליקציה פתוחה — ניסיון דחיפה חוזר, גם אם טיימר הרטריי הקודם אבד להשעיית-טאב.
- **דחיפה בחזרה לאפליקציה:** מעבר לרקע ובחזרה מפעיל סנכרון מיידי של כל מה שממתין.
- **רענון bundle אוטומטי:** אם העמוד רץ מעל 12 שעות (PWA שנשאר חי בזיכרון ימים — בדיוק מה שגרם לטלפון של אורי להריץ קוד ישן) והכל מסונכרן — טעינה-מחדש אוטומטית בחזרה לאפליקציה. תיקונים עתידיים יגיעו לכולם בלי "תסגרו ותפתחו".
- אומת בדפדפן (dev mode): כל 4 מצבי החיווי מוצגים נכון ב-RTL, מיקום מעל safe-area, לא חוסם מגע; האפליקציה עולה נקי בלי שגיאות קונסול.
### שני באגים במחיקות החוזרות של אורי — נמצאו ותוקנו (25/07, לילה)
**אורי דיווח שוב שהנתונים באייפון (PWA) נמחקים "שוב ושוב", וחשד שזה קשור לאדמין. החשד היה מוצדק חלקית — נמצאו שני באגים אמיתיים:**

**באג 1 — כשל רשת מתפרש כ"משתמש חדש" (הקטלני, פוגע בכולם):** בפתיחת האפליקציה, אם השאילתה "מה שמור בענן" נכשלת (קליטה חלשה, אופליין רגעי, טוקן שפג), הקוד התייחס לכישלון כאילו "אין שורה בענן = משתמש חדש" — מחק את העותק המקומי במכשיר, טען תבנית ריקה, ובחיבור הבא דרס גם את השורה בענן. פתיחת אפליקציה במעלית/רכבת = מחיקה מלאה. התיקון: כשל שאילתה מובחן עכשיו מ"אין שורה". בכשל — האפליקציה עולה מהעותק המקומי בלי לגעת בכלום; אם אין גם עותק מקומי — מסך "📡 אין חיבור לשרת" עם ניסיון חוזר כל 3 שניות (+ רענון טוקן כל ~15 שניות), במקום לנחש. מחיקת מקומי קורית רק כשהשרת ענה בפועל "אין שורה".

**באג 2 — התוכנית המשותפת פורסמה עם התוצאות האישיות של האדמין (ההסבר ל"רק האימון הראשון שורד"):** בכל שמירה של האדמין, הקוד פרסם ל-shared_program את השבועות המלאים שלו — כולל הלוגים האישיים. בפועל היה שמור שם צילום ישן של אורי עם האימון הראשון בלבד, וכל פעם שבאג 1 מחק אותו — הוא "שוחזר" בדיוק לצילום הזה. התיקון: פרסום התוכנית עובר stripLogs (מנקה done/תוצאות/דירוגים/סיכומים מכל יום כולל alt) — גם בפרסום וגם בזריעה למשתמש חדש (הגנה כפולה כי השורה הקיימת במסד עדיין מזוהמת).

**חיזוקים נלווים:** כשל בטעינת הלוח לא מרוקן יותר את myResults (נופל חזרה לעותק המקומי הקודם, ודחיפת הלוח בעליה מדולגת); רענון-לוח בזמן-אמת שנכשל שומר את הלוח האחרון התקין במקום להציג ריק; כשל בטעינת פרופיל לא מקפיץ שוב את שאלון מין/תאריך-לידה; כל שאר השאילתות עטופות כך שכשל רשת לא מפיל את הטעינה. כלי דיבוג: `window.__cfbyNet`, `window.__cfbySync`.
- אומת בדפדפן: האפליקציה עולה נקי, מסך אין-חיבור מוצג ומוסר תקין (RTL, מכסה מסך מלא).
- הנתונים שנמחקו לאורי היום אינם ניתנים לשחזור (השורה בענן נדרסה) — יזין מחדש; מכאן והלאה המסלול חסום.
### עדכון-עצמי של גרסאות + תווית build (25/07, לילה מאוחר)
**אורי דיווח "עדיין לא עובד". אומת ישירות מול הפרודקשן:** vercel.app מגיש את boot.js המתוקן על כל רכיביו — כלומר הבעיה היא (שוב, פעם שלישית) מכשיר שמריץ bundle ישן. הפתרון הקבוע:
- **public/version.json** — מוטבע בכל דיפלוי לצד קבוע BUILD ב-boot.js (חובה לזכור לעדכן את שניהם בכל דיפלוי!).
- **בדיקת עדכון אוטומטית:** בכל עליית אפליקציה ובכל חזרה מהרקע — fetch ל-version.json עם no-store; אם יש build חדש והכל מסונכרן → reload אוטומטי (עם נעילת לולאה: ניסיון אחד לכל build, ולעולם לא מעל נתונים לא-מסונכרנים). מחליף את היוריסטיקת ה-12 שעות. מהגרסה הזאת והלאה אף מכשיר לא יתקע על קוד ישן — נדרשת סגירה+פתיחה ידנית אחרונה אחת כדי לעלות עליה.
- **תווית גרסה גלויה:** "v2026-07-25.4" קטן ואפור בפינה הימנית-תחתונה — מסיים את סבבי "מה רץ אצלך בטלפון" במבט אחד.
- ניסיון לבדוק את שורת ה-states של אורי ישירות במסד (Management API דרך הכרום שלו) נחסם ע"י מסווג ההרשאות; החלופה: פאנל המשתתפים באפליקציה מציג "פעילות" (עדכון אחרון) לכל משתמש.
- הובהר לאורי: הארכיטקטורה כבר היום היא "שורה לכל משתמש בענן, נטען מכל מכשיר אחרי התחברות" — הבעיה הייתה בבאגים שדרסו את השורה, לא במודל האחסון.
### סרגל מאמץ נפרד מ-PR, שיאים לפי תרגיל, ומשקלי עבודה ב-Scaled (25/07) — v1.1.0
**שלושת הצעדים שאורי ביקש, כולם באפליקציה (אין שינוי במסד — הכל בתוך ה-JSON של המשתמש):**
- **סרגל המאמץ סודר:** PR הוצא מהסרגל — שיא הוא לא רמת מאמץ. הסרגל עכשיו תחושה בלבד: 😌 Easy / 💪 Good / 🥵 Hard / 🔥 Max Effort. במקומו נוסף טוגל נפרד "🏆 שברתי שיא באימון הזה" (שדה `pr` על היום), שאפשר להדליק לצד כל דירוג מאמץ. יום עם שיא מקבל תג 🏆 PR בכותרת ובשיתופים.
- **מיגרציה שקופה:** ימים ישנים עם `rating:'pr'` הופכים בטעינה ל-`pr:true` + סרגל פנוי; דירוגי Average/Missed ישנים עדיין מוצגים (LEGACY_RATINGS) ומוחלפים ברגע שבוחרים ערך חדש.
- **טאב שיאים שודרג — שיאים לפי תרגיל:** מדור חדש "🏋️ שיאים לפי תרגיל" — המשקל הכבד ביותר שנרשם אי-פעם ב-LIFT לכל תרגיל (קיבוץ לפי השם שלפני ה-"—", כך ש-"Back Squat — CapaciTest #1" נספר כ-Back Squat), עם שבוע, תאריך ומספר אימונים; לחיצה קופצת ליום. מתחתיו המדור הקיים "🏆 אימוני שיא" (הימים שסומנו בטוגל).
- **משקלי עבודה ב-Scaled:** צ'קבוקס ה-RX הפך לשני כפתורים — "עשיתי RX — נכנס לדירוג" ו"עשיתי Scaled". בחירת Scaled פותחת שדה חופשי "משקלי עבודה" (`log.scaled`, למשל "40kg במקום 60"). RX ו-Scaled סותרים זה את זה; מעבר ל-RX מסתיר את השדה אבל שומר את הטקסט. הדירוג נשאר RX בלבד — לא השתנה.
- ניקוי סקאפולד ב-boot.js מאפס גם את הדגלים החדשים (pr/scaled) כדי שתוכנית מפורסמת לא תגרור אותם.
- אומת בדפדפן (dev mode): הסרגל החדש, הטוגל, תג הכותרת, בלעדיות RX/Scaled + שמירת הטקסט, וטאב השיאים על שני מדוריו — הכל עובד, אפס שגיאות קונסול. גרסה: **1.1.0** (BUILD + version.json).

### סרגל המאמץ דויק ל-RPE 1-10 (25/07, המשך) — v1.2.0
**אורי ביקש לדייק את הסרגל; בחר מעבר לסקאלת RPE מספרית** — כמו שהתוכניות עצמן מדברות ("מאמץ 7 מ-10"):
- 10 כפתורים מספריים 1–10 במקום 4 כפתורי תחושה. צבע לפי טווח: 1–4 טורקיז, 5–6 ירוק, 7–8 כתום, 9–10 אדום.
- מתחת לסרגל כיתוב חי: "🥵 RPE 7 — קשה — נשאר בטנק" (RPE_DESC לכל ערך); בלי בחירה: "1 = קל מאוד · 10 = מאמץ מקסימלי".
- תג הכותרת והשיתופים מציגים "RPE 7" עם אימוג'י לפי טווח.
- מיגרציה: סקאלת התחושה קצרת-החיים מ-v1.1.0 וגם הסקאלה המקורית ממופות ל-RPE בטעינה (easy→3, good→6, average→5, hard→8, max→10). 'missed' נשאר תג ישן — הוא לא רמת מאמץ, לא ממציאים לו מספר.
- `rating` נשמר כמחרוזת '1'..'10'; `ratingMeta` מזהה ערכים מספריים ומייצר תווית/צבע/אימוג'י.
- אומת בדפדפן: בחירה, שמירה, כיבוי בלחיצה חוזרת, תג כותרת. גרסה: **1.2.0**.

### אינדורנס שהושלם = יום כחול (25/07, המשך) — v1.2.1
**אורי:** מי שעשה אינדורנס — זה האימון שלו לאותו יום (לכן ה-WOD ננעל), אבל היום לא נצבע כהושלם. **התיקון:** helper חדש `dayComplete(day)` = הושלם אחד משני הסשנים, והוחל בכל מקום שבדק רק את `day.done`:
- מפת החום, THIS WEEK והצ'יפים ביומן — יום עם אינדורנס שמור נצבע **כחול** עם ✓.
- `isMissed` — יום כזה לא נצבע יותר אדום כ"הוחמץ".
- רצף האימונים (streak) סופר אותו.
- תזכורת "אימונים שלא סומנו" — יום שהושלם בו סשן אחד לא מציק יותר על הסשן התאום (זה המודל: אחד מחליף את השני). בונוס: יום מנוחה גם לא נספר בתזכורת.
- שיתוף שבוע (טקסט + תמונה): היום מקבל ✅, הכותרת/תוצאה/RPE נלקחים מהסשן שבוצע בפועל (האינדורנס), והמונה "X/5 הושלמו" סופר אותו.
- (המונים הכלליים — סה"כ אימונים, יעד שבועי, נוכחות בדירוג — כבר ספרו alt קודם; לא השתנו.)
- אומת בדפדפן: שמירת אינדורנס ב-W1 SUN → התא במפה נהיה כחול (rgb(35,64,154)), המונה עלה, אפס שגיאות קונסול. גרסה: **1.2.1**.

### אבחון: "מחזיר אותי לחלונית מין/גיל + לא מסונכרן" (25/07 ערב)
**אורי:** מתאמן (Guy) נזרק שוב ושוב לחלונית המגדר/תאריך-לידה, התשובה לא נשמרת, והסקירה מציגה "⚠️ לא מסונכרן — מנסה שוב". **האבחון (בלי שינוי קוד):**
- נבדק מול המסד החי עם משתמש בדיקה זמני: הרשמה, upsert פרופיל (מין+תאריך) ו-upsert ל-states — **הכול עובד**. הסכימה וה-RLS תקינים.
- ציר הזמן חשף את הסיבה: הפרופיל של Guy (guymogly) נוצר ב-21:16 — **אחרי** צילומי המסך (21:14–21:15) — ומאז הוא שלם ומסתנכרן (board התעדכן 21:21). כלומר בזמן הצילומים הטלפון החזיק session של **חשבון קודם שנמחק מהמסד** (כנראה נמחק ידנית ב-Supabase אחרי שמחיקת האדמין נכשלה — ה-RPC החסר מאותו יום).
- מנגנון התקלה: JWT של משתמש מחוק נשאר תקף עד שעה. קריאות עדיין עוברות (מדיניות ה-select פתוחה לכל מחובר) — אז האפליקציה עולה כ"משתמש טרי" בלי פרופיל → החלונית מופיעה בכל פתיחה. אבל **כל כתיבה נכשלת** (FK ל-auth.users שכבר לא קיים) → החלונית "לא נשמרת" ותג "לא מסונכרן" דולק. ה-upsert של החלונית (boot.js:721) לא בודק שגיאה — לכן הכישלון שקט.
- הפתרון למתאמן: התנתקות/ניקוי נתוני אתר בטלפון → התחברות עם החשבון החדש. תוך שעה זה גם קורה מעצמו (הטוקן פג → מסך התחברות).
- חיזוקים מומלצים (לא בוצעו): בדיקת שגיאה ב-upsert של החלונית; push שנכשל שוב ושוב על 401/403/409 יוביל להתנתקות במקום ריטריי אינסופי; בדיקת שגיאה גם ב-upsert של ההרשמה (index.html:110).

### איפוס סיסמה מהאדמין (25/07, לילה) — v1.3.0
**אורי ביקש:** לראות בפאנל האדמין את הסיסמאות של כולם + לשנות סיסמה למי ששכח.
- **למה אי אפשר "לראות" סיסמאות:** Supabase שומר סיסמאות רק כ-hash (bcrypt) — חד-כיווני, בלתי הפיך, בכוונה. אין שום דרך לשלוף את הסיסמה המקורית, של אף אחד, כולל לאדמין. זה גם מגן על המתאמנים — אנשים ממחזרים סיסמאות (אותה סיסמה גם למייל/בנק), אז לא שומרים אותן גלויות.
- **מה כן נבנה — איפוס סיסמה:** כפתור "🔑 סיסמה" ליד כל משתמש בפאנל המשתתפים (כולל האדמין עצמו). לחיצה → הזנת סיסמה חדשה (6+ תווים) → הסיסמה מוחלפת מיד, וההודעה מציגה אותה כדי למסור למתאמן. זה פותר את תרחיש "שכחתי סיסמה" במלואו.
- **מאחורי הקלעים:** RPC חדש `admin_set_password(target, new_password)` ב-`supabase/schema.sql` — SECURITY DEFINER שבודק `is_admin()`, מעדכן את ה-hash ב-auth.users (crypt/bf), ומוחק refresh tokens ישנים כך שסשן ישן של המשתמש מת עם האיפוס (בכניסה הבאה יתחבר עם הסיסמה החדשה).
- אם ה-RPC עוד לא הורץ במסד — ההודעה באדמין אומרת במפורש להריץ את schema.sql (כמו במחיקה).
- אומת בדפדפן: האפליקציה עולה נקי, אפס שגיאות קונסול (בדיקת הכפתור עצמו דורשת התחברות אדמין מול המסד החי — אורי יבדוק אחרי הרצת ה-SQL). גרסה: **1.3.0**.

### דשבורד: כרטיס "המקום שלי" + גרף התקדמות לשיא (26/07) — v1.4.0
**אורי ביקש:** בסקירה, במקום "רצף שיא (ימים)" — כרטיס המקום שלי בדירוג; ולבנות גרף התקדמות לכל תרגיל.
- **כרטיס "המקום שלי בקטגוריה"** בסקירה: המקום העונתי בתוך הקטגוריה (למשל ‎2/12) + שורת דלתא מול שבוע שעבר — "▲ עלית מקום אחד משבוע שעבר" בירוק, ▼ ירידה באדום, = ללא שינוי באפור. לחיצה על הכרטיס קופצת לטאב הדירוג. אין קטגוריה או דירוג עדיין → "—" בלי דלתא.
- החישוב: אותו מנוע ניקוד עונתי — הדירוג המצטבר "נכון לעכשיו" מול הדירוג המצטבר עד סוף השבוע הקודם (helper חדש `rankThrough`). גם "מיקום בקטגוריה" בכרטיס הפרופיל עבר להשתמש בו (אותה תוצאה, קוד אחד).
- רצף השיא ירד מהסקירה אבל עדיין מחושב מאחורי הקלעים (מזין את תג "רצף 5").
- **גרף התקדמות לפי תרגיל (טאב שיאים):** לחיצה על כרטיס תרגיל פותחת מודל עם גרף קו (SVG טהור, בלי ספריות) של כל המשקלים שנרשמו לתרגיל לאורך התוכנית — נקודת השיא מודגשת בכתום, ריחוף מציג שבוע/תאריך/חזרות, וכפתור "← פתח את אימון השיא" קופץ ליומן של אותו יום. תרגיל עם רישום בודד מציג נקודה אחת + הסבר שהגרף יתמלא עם הזמן. על כל כרטיס נוסף רמז "📈 לחץ לגרף ההתקדמות".
- אומת בדפדפן עם עמוד הרצה זמני (נתוני דמו, עקיפת התחברות; נמחק אחרי הבדיקה): הכרטיס הציג ‎2/3 עם דלתא ירוקה, הגרף צייר 4 נקודות (100→105→102.5→110) עם תוויות שבועות, הניווט לאימון השיא עבד, אפס שגיאות קונסול. גרסה: **1.4.0**.

### היערכות ל-100 משתתפים: גיבוי/שחזור + ריסון רענוני הדירוג (26/07, המשך) — v1.5.0
**אורי שאל:** גדלים מ-50 ל~100 משתתפים, התחברות בשם-משתמש+סיסמה בלבד — האם זו בעיה? **התשובה בקצרה:** Vercel לא מהווה בעיה בכלל (אתר סטטי, המגבלה החינמית רחוקה פי מאה); Supabase בסדר גם ב-100 (מגבלת המשתמשים 50,000), והתחברות בלי אימייל עובדת זהה בכל קנה מידה — המחיר היחיד הוא שכל "שכחתי סיסמה" עובר דרך האדמין. מה שכן דורש היערכות: אין גיבוי אוטומטי בתוכנית החינמית, ותעבורת הנתונים (egress, מגבלה 5GB/חודש) שווה מעקב חודשי תחת Usage בדשבורד של Supabase. אם מתקרבים — Supabase Pro (25$/חודש, כולל גיבוי יומי אוטומטי).
- **💾 גיבוי לקובץ (חדש בפאנל המשתתפים):** כפתור שמוריד את כל המסד — פרופילים, תוצאות (states), לוח, והתוכנית המשותפת — לקובץ JSON אחד במכשיר (`batyam-backup-תאריך.json`). גיבוי חלקי נחשב כישלון (עדיף כלום מגיבוי שנראה שלם ואינו). לצד הכפתורים כיתוב "גיבוי אחרון: לפני X ימים" שנצבע כתום אחרי שבוע בלי גיבוי או אם טרם נעשה גיבוי מהמכשיר.
- **♻️ שחזור מגיבוי:** בחירת קובץ גיבוי → אישור מפורש (תאריך הגיבוי + כמה משתמשים, אזהרה שנתונים קיימים יידרסו) → שחזור שורה-שורה עם ספירה. משתמש שנמחק מאז הגיבוי פשוט מדולג (ולא מפיל את כל הטבלה). אין צורך בשינוי SQL — הרשאות האדמין הקיימות מכסות הכל.
- **ריסון רענוני הדירוג בזמן-אמת:** עד עכשיו כל שמירה של כל מתאמן גרמה לכל טלפון פתוח למשוך מחדש את הלוח המלא — בערב עמוס עם 100 איש זה עשרות משיכות לדקה לכל מכשיר. עכשיו רענון הלוח מוגבל לאחד לכל 15 שניות למכשיר (אירועים בינתיים "רוכבים" על הרענון הממתין). קריאה בלבד — שמירת התוצאות של המשתמש עצמו לא מושפעת ולא מתעכבת.
- מחיקת משתמש מלאה (`admin_delete_user`) — כבר נבנתה ב-v1.3.0; עדיין ממתינה להרצת schema.sql במסד (ראו "פתוח").
- אומת בדפדפן (dev mode): האפליקציה עולה נקי עם v1.5.0, אפס שגיאות קונסול. בדיקת כפתורי הגיבוי עצמם דורשת התחברות אדמין — אורי: פתח את פאנל המשתתפים ולחץ "💾 גיבוי לקובץ" אחרי הדיפלוי. גרסה: **1.5.0**.

### תיקון סדר אימוני שבוע 3 + אינדורנס ראשון (26/07, המשך) — v1.5.1
**אורי שלח צילומי אימונים לתיקון — הסדר בשבוע 3 (26/07–01/08) היה שגוי.** הוסכם: כל הטמעת אימונים מקבלת מעכשיו העלאת גרסה בספרה השלישית (1.5.0 → 1.5.1 וכן הלאה).
- **הזיהוי** (לפי הבריפים בצילומים — "התפריט שפותח לנו את השבוע" / "שישי" / מבנה METCON-B): שלושת הימים הוחלפו במעגל —
  - **ראשון 26/07:** יום הסנאץ' (Squat Snatch + Hang @RPE, מטקון ‎3×AMRAP 4:00 קלוריות+קלאסטרים, אקסטרה Back Squat כבד) — היה קודם בשישי.
  - **חמישי 30/07:** Power Clean + Hang Squat Clean + ‎6 RFT (ריצה/ברפיז/פרונט סקווט) + Pull Up Builder — היה קודם בראשון.
  - **שישי 31/07:** Power Snatch EMOM + ג'ימנסטיקס "Next Level" + קונדישנינג High Output — היה קודם בחמישי.
- **נוסף סשן האינדורנס החלופי לראשון** (חסר עד עכשיו בשבוע 3): ‎4×AMRAP 4:00 — ‎300m חתירה buy-in ואז וולבולס/ברפי-בוקס-ג'אמפס (ניקוד סבבים+חזרות), ו-METCON-B: ‎3×800m ריצה עם 3 דקות מנוחה (ניקוד זמן ריצה כולל). נעילת "סשן אחד ביום" חלה אוטומטית.
- תוקן באג עדין בדרך: ליום חמישי החדש יש אקסטרה אחד ואילו לישן היו שניים — בלי ריקון מפורש, `applyProgram` היה משאיר למשתמשים קיימים את האקסטרה הישן ("2:30 Work") ביום חמישי. נוסף `['','']` מנקה.
- PROGRAM_VERSION ‎10→11 — מתעדכן אוטומטית אצל כולם בלי לפגוע בתוצאות שנרשמו.
- אומת בדפדפן: התוכנית המעודכנת נטענת, כל 7 הימים במקומם, resultMode תקין בכל המטקונים, אפס שגיאות קונסול. גרסה: **1.5.1**.

### תיקון שישי שבוע 3: הקונדישנינג הוא המטקון (26/07, המשך) — v1.5.2
**אורי עבר איתי אימון-אימון (ראשון ✓, חמישי ✓ — כבר היו נכונים) ותיקן את שישי 31/07:** המטקון של היום הוא הקונדישנינג, לא הג'ימנסטיקס.
- **METCON:** Conditioning — High Output, EMOM 12:00 (דקה עבודה חתירה/אופניים/סקי, דקה מנוחה). ניקוד: סך קלוריות/מטרים (`amount`).
- **הג'ימנסטיקס "Next Level"** עבר לבלוק מדורג שני (metcon2, כמו שני החלקים של יום רביעי) — הציון "total reps" שלו עדיין נרשם ונשמר להשוואה מול שבוע שעבר.
- ה-EXTRA נשאר "2:30 Work / 1:30 Rest x 3" + ריקון מפורש של הסלוט השני (אותו gotcha של applyProgram מ-1.5.1).
- PROGRAM_VERSION ‎11→12. אומת בדפדפן: מבנה שישי תקין (lift/metcon/metcon2/extra), אפס שגיאות. גרסה: **1.5.2**.

### תגים מורחבים + פרופיל ציבורי + פופאפ בלוק (26/07, ערב) — v1.6.0
**שלושה פיצ'רים שאורי ביקש, בסדר הזה:**
1. **מערך תגים מורחב — 6 → 15 תגים**, מדורגים: אבני דרך (1/10/25/40 אימונים), שיאים (1/5/10), רצף (5/10 ימים), התמדה (שבוע מלא / 3 שבועות / בלוק מושלם=8), RX (ראשון / 10 מטקונים), מאמץ (5 אימוני RPE 9+). ההגדרות ב-`badgeList(c)` אחד שמשרת גם את הפרופיל שלי וגם פרופילים של אחרים — אותם ספים בשני המקומות.
2. **פרופיל ציבורי בלחיצה על שורה בדירוג:** מודאל עם שם, מין, גיל, קטגוריה, מיקום בקטגוריה, 15 התגים ו-3 שיאים אחרונים. הנתונים מגיעים מעמודת `pub` חדשה בטבלת `board` — **תקציר קטן שכל משתמש מפרסם על עצמו** בכל סנכרון (`publicSummary()` ב-boot.js: מונים + 3 שיאים בלבד; היומן המלא נשאר פרטי ב-states). מתאמן שעוד לא נכנס בגרסה החדשה → המודאל מציג הערה שהנתונים יופיעו בקרוב.
3. **פופאפ הודעת בלוק + קונפטי 🎖️:** אזור חדש בפאנל האדמין ("📣 הודעת בלוק חדש") — כותרת + טקסט חופשי, פרסום/תצוגה מקדימה/הסרה. ההודעה נשמרת ב-`shared_program.announcement` עם id ייחודי; כל מתאמן רואה אותה **פעם אחת בלבד** (נרשם ב-`profiles.announcement_seen` + גיבוי localStorage). בפתיחה: פרץ 90 חתיכות קונפטי (CSS טהור, בלי ספריות). **חשוב: כרגע אין הודעה במסד — אף אחד לא יראה כלום עד שאורי יפרסם אחת עם הבלוק הבא.** פרסום חדש (id חדש) מציג את הפופאפ שוב לכולם.
- אומת מקומית (dev=1): דירוג לחיץ, מודאל עמית עם/בלי נתונים, 15 תגים בפרופיל שלי, פופאפ+קונפטי נפתח ונסגר, אפס שגיאות קונסול. גרסה: **1.6.0**.
- **v1.6.1 (מיד אחרי):** נבדק מול המסד החי (בדיקת PostgREST עם מפתח ה-anon) — שלוש העמודות עוד לא קיימות, והקוד החדש היה שובר את טעינת הלוח והסנכרון עד הרצת ה-SQL. נוספה עמידות מעבר: כל שאילתה/דחיפה שנופלת על עמודה חסרה מנוסה שוב בלעדיה. האפליקציה עובדת מלא גם לפני המיגרציה; הפיצ'רים החדשים מתעוררים ברגע שה-SQL רץ.

### דארק מוד 🌙 (26/07, המשך) — v1.6.12
**אורי ביקש:** שהאפליקציה תתאים את עצמה למצב הבהיר/כהה של הטלפון. אופיין יחד לפני הביצוע: אוטומטי כברירת מחדל + מתג ידני, ערכה כהה אפור-שחור ניטרלי (לא נייבי), תמונות שיתוף תמיד בהירות, בלי שאלה בהרשמה — רק אזכור במדריך.
- **שלושה מצבים — אוטומטי / בהיר / כהה:** ברירת המחדל אוטומטי עוקבת אחרי הגדרת הטלפון (`prefers-color-scheme`), כולל מעבר חי באמצע שימוש (הטלפון מתחלף בערב → האפליקציה מיד איתו). המתג בתחתית מודל הפרופיל (👤). הבחירה נשמרת מקומית במכשיר (`cfby_theme` ב-localStorage, בכוונה מחוץ ל-state המסונכרן; העברה לסנכרון בעתיד = כמה שורות).
- **הריפקטור מאחורי הקלעים:** ~140 צבעים קשיחים בכל רוחב app.html הוחלפו במשתני CSS (‎--card, --paper, --field, --ink ועוד ~25). שתי הפלטות מוגדרות ב-`:root` / `html[data-theme="dark"]`; boot.js קובע את ה-data-theme עוד ב-`<head>` — אין הבזק לבן בפתיחה. הערכה הכהה: רקע כמעט שחור, כרטיסים אפור כהה, כחול מובהר לקריאות; הכתום נשאר. הכותרת, מסך הטעינה, ההתחברות ודף האישור נשארים נייבי ממותג בשני המצבים (בכוונה — הם כהים ממילא).
- **תמונות שיתוף (שתף שבוע/אימון) תמיד בהירות:** ברגע הצילום נכפית הערכה הבהירה ומוחזרת מיד — מראה אחיד בוואטסאפ לכל הקבוצה.
- **מדריך:** שורת הפרופיל מזכירה עכשיו גם את מצב התצוגה.
- אומת מקומית (dev): סריקה אוטומטית של כל הטאבים והמודלים במצב כהה — אפס אלמנטים בהירים שנשכחו ואפס טקסט-כהה-על-כהה; המתג עובד בשלושת המצבים ונשמר; מצב בהיר זהה פיקסל-פיקסל לערכה המקורית; אפס שגיאות קונסול. גרסה: **1.6.12** (אורי בחר לשמור את הדחיפה במשפחת ‎1.6).
- **תיקון בעקבות בדיקת אורי במחשב:** מודלי הפרופיל והמדריך נחתכו בחלון נמוך בלי אפשרות גלילה (המתג לא נראה). ארבעה מודלים (פרופיל, מדריך, היכרות, גרף שיאים) קיבלו גלילה פנימית — `max-height:calc(100vh - 40px)` + `overflow:auto`, כמו מודל העמית שכבר עבד. אומת בגבהים 600/450px ובמובייל 375×812: גלילה עובדת, המתג נגיש ולחיץ, אפס רגרסיות בסריקות הדארק בכל הטאבים.

### אבחון: תוצאת אינדורנס מהטלפון לא נשמרה (26/07) — ללא שינוי קוד
**אורי דיווח:** הזין תוצאת אינדורנס בטלפון, סגר אחרי כמה דקות — לא נשמר. הזנה מהמחשב כן עבדה והסתנכרנה לטלפון. נבדק: הפריסה תקינה (פרודקשן מגיש 1.6.12), לוגיקת השמירה המקומית והסנכרון תקינות בקוד.
- **חשוד עיקרי:** הדחיפה מהטלפון נכשלה (קליטה), ואז פתיחת המחשב כתבה לשרת חותמת חדשה יותר — והטלפון קיבל את עותק השרת ואיבד את ההזנה. זה חור מובנה במודל "הקובץ השלם האחרון מנצח": חותמת ה-dirty מגינה רק מפני עותק שרת *ישן*, לא מפני מכשיר אחר שכתב אחרי הכישלון.
- חשודים משניים: השמירה לא הושלמה בטלפון (כפתור 💾 לא נלחץ / נחסם ע"י כלל "אימון אחד ביום אינדורנס"), או באנדל ישן בטלפון.
- **הפתרון המלא** אם זה יחזור: מיזוג ברמת הימים (חותמת זמן לכל יום, כל יום שורד מהמכשיר שעדכן אותו אחרון) במקום דריסת הקובץ השלם. **הוחלט (אורי): לא משקיעים בזה כרגע** — ממתינים לראות אם התקלה חוזרת.
- אם זה חוזר, לבדוק בטלפון: תגית הגרסה (ימין-למטה, אמור v1.6.12), האם הופיע "✓ נשמר בענן" אחרי השמירה, והאם היום התחלף למסך "✅ האימון נשמר".

### פאנל המשתתפים במחשב: נבעט מכל קליק + אין גלילה (26/07, המשך) — v1.6.13
**אורי דיווח:** בפאנל ניהול המשתמשים דרך המחשב אי אפשר לגלול למטה, וכל לחיצה "ברחבי המסך" סוגרת את הפאנל.
- **הסיבה:** לפאנל הייתה סגירה-בלחיצה-על-הרקע, אבל לקופסת התוכן (מוגבלת ל-1000px במרכז) אין רקע משלה — כל המסך נראה כמו פאנל כהה אחד. במסך רחב, כל קליק בשוליים (שנראים חלק מהפאנל) פגע ברקע וסגר. ובנוסף — פס הגלילה שייך לשכבת הרקע עצמה, ובכרום/ווינדוס לחיצה על פס הגלילה נחשבת קליק על הרקע → כל ניסיון לגלול עם העכבר סגר את הפאנל. בטלפון אין פס גלילה והקופסה ממלאת את כל הרוחב — לכן זה הופיע רק במחשב.
- **התיקון (boot.js):** בוטלה סגירת-הרקע לגמרי — סוגרים רק עם כפתור "✕ סגור" או מקש Escape (נוסף). הגלילה בגלגלת ובפס הגלילה עובדת עכשיו חופשי.
- גרסה: **1.6.13** (אורי אישר).
- **המשך (v1.6.14):** אורי בדק — הבעיטות נעלמו אבל הגלילה עדיין לא עבדה: פס הגלילה שנראה במסך היה של **האפליקציה שמאחורי הפאנל** (שני פסים באותה פינה ימנית; של הפאנל היה כמעט בלתי נראה — כהה על כהה). הגרירה גללה את האפליקציה החבויה בזמן שהפאנל עמד במקום. תוקן: כשהפאנל נפתח — גלילת העמוד שמאחור ננעלת (ומשוחררת בסגירה), כך שנשאר פס גלילה אחד בלבד — של הפאנל — והוא גם קיבל עיצוב בהיר ובולט.

### אימונים לא נשמרים אצל כמה מתאמנים + אין תגית גרסה (26/07, המשך) — v1.6.15
**אורי דיווח:** דחיפת אימונים מהנייד לא עובדת אצל כמה מתאמנים (לא רק אצלו), בלי חיווי "נשמר בענן", והנתונים נעלמים גם מהמכשיר עצמו. בנוסף — תגית הגרסה לא נראית אצלו בכלל, גם בדפדפן אחרי סגירה מלאה.
- **האבחנה:** התסמינים תואמים אחד-לאחד את הבאגים שתוקנו רק ב-25/07 (תג סנכרון, מחיקה-בטעות כשהרשת קרטעה בפתיחה, debounce שנהרג בסגירת האפליקציה) — כלומר הטלפונים תקועים על **באנדלים ישנים מלפני התיקונים**. ביצה ותרנגולת: מנגנון העדכון האוטומטי (version.json) קיים רק בקוד החדש, אז מי שרץ על קוד ישן לא בודק עדכונים לעולם, ו-PWA מותקן נשאר "חי" ברקע ימים. ותגית הגרסה הישנה הייתה ממילא כמעט בלתי נראית — טקסט שחור-שקוף 9px בפינה, נעלם לגמרי בדארק מוד, וגם נמחק כשהאפליקציה בנתה מחדש את המסך.
- **שלושה תיקונים (v1.6.15):**
  1. **תגית גרסה בתחתית הדף** (בקשת אורי): "גרסה 1.6.15" בסוף העמוד הנגלל, צבע `--muted` שמתחלף עם הערכה (נראה בבהיר ובכהה), ו-MutationObserver שמחזיר אותה אם הרינדור של האפליקציה זרק אותה.
  2. **סגירת פרצת הבאנדלים התקועים לתמיד (sw.js v2):** הדפדפן בודק מעצמו אם sw.js השתנה (בניווטים, ולעמודים חיים תוך ~24 שעות) — זה ערוץ העדכון היחיד שמגיע גם למכשירים שלא טוענים את הדף מחדש לעולם. ה-SW החדש, ברגע שהוא מותקן, שולח פינג לכל עמוד פתוח: עמוד שרץ קוד עדכני עונה ומתעדכן בנימוס (דרך checkFreshBundle — לעולם לא מעל נתונים לא מסונכרנים); עמוד שלא עונה תוך 3.5 שניות = באנדל ישן → ניווט כפוי לרענון. רענון בטוח: localStorage וחותמת ה-dirty שורדים, והקוד החדש שנטען מזהה ודוחף נתונים שלא סונכרנו. בנוסף boot.js מזרז את בדיקת ה-SW בכל חזרה לאפליקציה (`registration.update()`).
  3. **התראה על כשל שמירה מקומית:** עד עכשיו אם `localStorage.setItem` נכשל (אחסון מלא/חסום) האפליקציה בלעה את השגיאה בשקט — המתאמן ממשיך להזין וכלום לא נשמר, אפילו לא במכשיר. עכשיו קופץ באדג' אדום "⚠️ השמירה במכשיר נכשלה".
- אומת מקומית (dev): התגית בתחתית הדף, שורדת רינדור-מחדש, צבע מתחלף בדארק; ה-SW החדש נרשם ומופעל בלי שגיאות, ועמוד עדכני עונה לפינג ולא מרוענן בכפייה; אפס שגיאות קונסול.
- **מה אמור לקרות אחרי הדיפלוי:** תוך יום-יומיים של שימוש כל טלפון תקוע יתרענן מעצמו לגרסה החדשה. מי שסוגר ופותח מלא — מיד. סימן הצלחה: כל מתאמן רואה "גרסה 1.6.15" בתחתית הדף ו"✓ נשמר בענן" אחרי שמירה.
- **המשך (27/07) — "אולי יש בעיה בענן?":** אימון של אורי מ-23:28 נעלם שוב למרות שהטלפון על 1.6.15 והאפליקציה אישרה שמירה במכשיר. הענן נבדק מקצה לקצה עם משתמש בדיקה זמני (**test-claude-sync2** — למחוק מפאנל המשתתפים יחד עם test-claude-delete-me): הרשמה, דחיפת states (הוספה + עדכון), קריאה חזרה, דחיפת board — הכול תקין, בדיוק במסלול ה-REST של האפליקציה. שורות הבדיקה נמחקו. מסקנה: הענן בריא; ההסבר שנותר הוא דריסה בין-מכשירית (המודל "המחברת השלמה האחרונה מנצחת") — הפתרון המלא הוא מיזוג ברמת הימים, ממתין לאישור אורי.

### הבדיקה המקיפה: למה אימונים מהנייד נעלמים — ושלושת התיקונים (27/07) — v1.7.0
**אורי דיווח:** משתמשת חדשה שנרשמה מהאייפון, סימנה אימונים — והם נעלמו. בלי מחשב, בלי מכשיר שני. דרש בדיקה מקיפה של הקוד והענן. נמצאה שרשרת של שלושה כשלים:
1. **ערוץ החירום של רגע הסגירה מת אצל כולם (נמדד):** קובץ הנתונים שוקל **69.6KB לפני אימון ראשון**, וערוץ ה-keepalive (הדחיפה שאמורה לשרוד סגירת אפליקציה) מוגבל קשיח ל-64KB — נכשל בשקט ב-100% מהסגירות מאז שהתוכנית גדלה. יחד עם השהיית ה-800ms של הדחיפה הרגילה: "הזנתי וסגרתי" = הנתונים נשארו רק במכשיר.
2. **המחיקה העיוורת של "אין שורה בשרת" — הבאג של המשתמשים החדשים:** משתמש חדש שכל הדחיפות שלו בערב הראשון נכשלו (קליטה בבוקס / iOS השהה את הדף) מעולם לא קיבל שורת states בענן. בפתיחה הבאה השרת עונה "אין שורה" → הקוד הישן הסיק "משתמש טרי" ומחק את המכשיר — כולל חותמת ה-dirty — בלי לבדוק אותה. כל הערב הראשון נמחק מכל מקום. (אצל ותיקים עם שורה בענן ההגנה מ-24/07 תפסה — לכן זה פגע דווקא בחדשים.)
3. **הדריסה הבין-מכשירית:** כל דחיפה מחליפה את כל המחברת; שני הקשרי אחסון (טלפון+מחשב, ובאייפון אפילו ספארי מול האפליקציה מהמסך הראשי — שני אחסונים נפרדים!) דורסים זה את זה.
**התיקונים (boot.js בלבד, בלי שינוי במסד):**
- "אין שורה בשרת" + dirty מקומי ⇒ המקומי נשמר ונדחף (יוצר את השורה) במקום להימחק. מסלול המחיקה של אדמין נשאר: מחיקת שורות כשהמכשיר של המשתמש סגור ומסונכרן.
- **מיזוג ברמת הימים:** כל יום נושא חותמת "עודכן לאחרונה" (מפת `cfby_dts_v1`, מפתח `week_day`), החותמות נוסעות בתוך בלוב ה-states כ-`tracker._dts` (boot מסיר את השדה לפני שהאפליקציה רואה אותו). בכל פתיחה — מיזוג יום-יום לפי החותמות, תמיד (גם כשהמכשיר "נקי"), כך שגם דריסת-ענן מאתמול מתרפאת בפתיחה הבאה של הטלפון שמחזיק את היום. חתימת יום (logSig) מכסה רק שדות לוג של המשתמש — עריכות תוכנית של אדמין לא נחשבות שינוי; stringify ממוין-מפתחות כי jsonb לא שומר סדר; לוג-מעטפת ריק ≡ אין לוג (שיום שהאפליקציה רק אתחלה לא ינצח יום שנרשם בו אימון); יום חדש ריק מצמיחת תוכנית לא נחתם. שוברי שוויון (אין חותמות = נתוני 1.6.x) נופלים בדיוק לכלל הישן — מעבר חלק.
- חיווי: "⏳ שומר…" מופיע מיד עם הכתיבה (לא אחרי 800ms), "✓ נשמר בענן" מוצג 3 שניות (היה 1.6).
- אומת: 15/15 בדיקות יחידה על מנוע המיזוג (הקוד האמיתי מהבאנדל, כולל תרחישי סדר-מפתחות-jsonb, מעטפות ריקות, צמיחת תוכנית, alt); הלוך-חזור של `_dts` מול הענן החי במילישניות מדויקות; האפליקציה עולה נקי ב-dev עם תג "גרסה 1.7.0"; אפס שגיאות קונסול. דו-קיום עם 1.6.x בזמן הפריסה בטוח (מכשיר ישן מאבד חותמות אבל לא שובר כלום — ומתעדכן מהר דרך version.json).
- **ה-keepalive של 64KB נשאר מת בכוונה** — עם המיזוג בפתיחה הוא מיותר; תיקון אמיתי שלו (RPC שמקבל רק ימים שהשתנו) נרשם כרעיון עתידי.
- **אימות מקצה-לקצה בפרודקשן (27/07 ערב), אחרי דיווח נוסף של אורי:** התחברות כמשתמש הבדיקה לאפליקציה החיה → הזנת אימון דרך מסלול השמירה האמיתי (חיווי "שומר…" מיידי → "נשמר בענן", שורת states + חותמות בענן) → **סימולציית הדריסה המדויקת**: מכשיר ישן דרס את הענן עם עותק בלי האימון וחותמת-זמן חדשה יותר → פתיחה מחדש → **האימון קם לתחייה במכשיר והענן נרפא**, כולל החותמות. אגב הבדיקה נמצא ש-cleanDone של האפליקציה מבטל ✓ של יום לא-שלם (חסרה תוצאת מטקון) — התנהגות מכוונת, לא באג סנכרון. פוליש קטן: ימים שלא נגעו בהם לא מקבלים חותמת-אפס במפה (הועלה בלי הקפצת גרסה — אין שינוי התנהגות). נתוני הבדיקה נוקו; פרופיל משתמש הבדיקה סומן "🧪 משתמש בדיקה — למחוק" כדי שיהיה ברור בפאנל עד שאורי ימחק.
- **תמונת מצב מהלוח החי (27/07):** הדחיפות של אורי נוחתות (5 אימונים, עדכון אחרון 18:17); אושר בן שהם 7 אימונים — בריא; מאי עוזיאל 0 אימונים ועדכון אחרון 26/07 22:58 — כנראה המשתמשת שאיבדה את האימונים, טרם פתחה את האפליקציה מאז 1.7.0 (אם הנתונים עוד על הטלפון שלה — הפתיחה הבאה תציל ותדחוף אותם); אורן עוזיאל רשום אך ללא שורת לוח כלל — כנראה מעולם לא התחבר.

### תיקון אימון יום שני שבוע 3: החלפה בין המטקון לאקסטרה (27/07)
**אורי שלח צילום של התוכנית המקורית:** בהטמעה הראשונה החלוקה התהפכה — ה-Conditioning Mono Session הוזן כמטקון המרכזי וה-Blurred Strength כאקסטרה, בעוד שבתוכנית המקורית ה-Blurred Strength הוא הסשן המרכזי (תחת "Strength") וה-Mono Session הוא ה-Extra.
- **התיקון (app.html, שבוע 3 יום 1):** הוחלפו הסלוטים. המטקון עכשיו Blurred Strength (score: Weight used, resultMode: amount — מזינים את המשקל האחיד שנבחר לשתי התנועות), וה-Mono Session עבר לאקסטרה עם כל פירוט הרמות והנחיות המנוחה.
- `PROGRAM_VERSION` הוקפץ 12 → 13 כדי שהתיקון יגיע גם למכשירים עם סטייט קיים (הלוגים נשמרים — applyProgram דורס רק שדות תכנית).
- **בלי שינוי מספר גרסה** (בקשת אורי) — ה-SW הוא network-first אז הקוד מגיע גם כך.
- **לתשומת לב:** מי שכבר הספיק לרשום תוצאה למטקון של היום (זמן ל-Mono Session) — הלוג שלו יופיע עכשיו תחת Blurred Strength ויצטרך תיקון ידני.

### הסרטון מהנייד: אימון נשמר ונעלם מול העיניים — האבחון והתיקון (27/07, ערב) — v1.7.1
**אורי שלח סרטון מסך (18:16-18:17):** הזנת אימון שישי שבוע 2 בדיעבד מספארי באייפון (Power Snatch 45 ק"ג, מטקון 5 סבבים+4, RX, RPE 5) → "האימון נשמר והושלם 🎉", היומן קופץ ל-2/5 ושישי מקבל ✓ → מעבר לסקירה (6/40, המפה מראה את 24 כחול) → **הדף עושה reload לבד** → הכל חוזר לאחור: 5/40, שישי ריק, W2 1/5. פירקתי את הסרטון ל-73 פריימים ואיתרתי את רגע ה-reload המדויק (ספינר + מסך פתיחה בין שני מצבים).
**האבחון (סימולציה שהריצה את פונקציות הסנכרון האמיתיות מ-boot.js על ציר הזמן של הסרטון):**
- מיזוג-הימים של 1.7.0 **תקין**: בכל תרחיש של דחיפה אבודה או דריסת-שרת ממכשיר אחר — האימון שורד את ה-reload. נבדק, מוכח.
- התרחיש היחיד שמשחזר את הסרטון אחד-לאחד: **כתיבות localStorage מתות בשקט באמצע הסשן** (iOS — מכסה/מצב מוגבל). ואז כל השרשרת קורסת יחד: האפליקציה בולעת את הכשל (catch ריק) וממשיכה מהזיכרון; `doPushState` קורא את הנתונים **מהאחסון** (המת) ודוחף עותק ישן; החותמות וה-dirty לא נכתבים; וההתרעה "השמירה במכשיר נכשלה" (מ-1.6.15) מוצגת **מאחורי סרגל הכלים התחתון של ספארי** — בלתי נראית. עדות תומכת מהלוח החי: שורת הלוח של אורי הראתה 5 אימונים בעדכון 18:17 — הדחיפה יצאה, אבל נשאה את העותק הישן.
- **טריגר ה-reload זוהה גם הוא:** מנגנון החילוץ של ה-SW מ-1.6.15 — בדף שנטען ללא SW שולט (ביקור ראשון בספארי), הפינג של ה-SW יכול להגיע לפני ש-claim() מתעדכן בדף → הדף לא עונה "אני חי" → ה-SW עושה לו reload כפוי למרות שהוא מריץ את הגרסה העדכנית.
**התיקונים (v1.7.1):**
1. **מראה בזיכרון כמקור אמת לדחיפות** (boot.js): ה-interceptor שומר את ערך הכתיבה, החותמות וה-dirty גם בזיכרון; `doPushState`/`keepalivePush` דוחפים מהזיכרון, האחסון רק fallback. גם עם אחסון מת לגמרי — האימון + חותמותיו מגיעים לענן, וה-reload הבא **משחזר אותו מהשרת**. הוכח בסימולציית קצה-לקצה.
2. **אימות כתיבה בקריאה-חוזרת**: כשל כתיבה (כולל כשל שקט בלי exception) מציג את התרעת ה"השמירה במכשיר נכשלה" — שהוזזה **מעל סרגל הכלים של ספארי** (88px בדפדפן; בפוזיציה הישנה רק ב-PWA מותקן).
3. **דחיפות states סדרתיות**: דחיפה איטית ישנה לא יכולה יותר לנחות אחרי חדשה ולדרוס אותה בשרת (מרוץ ה-800ms).
4. **תשובת "אני חי" ל-SW בשני ערוצים** (controller + getRegistration + על controllerchange) — סוגר את מרוץ ה-claim שגרם ל-reload הכפוי המיותר.
5. **באג צד שנמצא בדרך:** `normalizeDay` לא העתיק את דגל `rest` — כל פתיחת אפליקציה מחקה את כל סימוני "🛌 מנוחה" ואף החתימה את הימים האלה מחדש כ"נערכו הרגע" (חותמת שמנצחת מיזוגים). תוקן (app.html).
- אומת: קומפילציה נקייה, האפליקציה עולה ב-dev בלי שגיאות קונסול, ההתרעה מרונדרת במיקום החדש.
- **סבב הקשחה לפני הפריסה (ביקורת מרובת-סוכנים על ה-diff + אימות ידני):** (א) מפת החותמות `_dts` ב-payload הועתקה כ-snapshot — בלי זה, ניסיון-חוזר של דחיפה (JWT פג) יכול היה לשלוח נתונים ישנים עם חותמת חדשה שמנצחת מיזוגים; (ב) גם `doPushBoard` דוחף מהמראה בזיכרון — הלוח לא נדחף ישן כשהאחסון מת; (ג) זריעת ה-boot כותבת קודם למראות ועטופה ב-try/catch — אחסון שזורק (מכסה מלאה) כבר לא מפיל את האתחול; (ד) watchdog משחרר דחיפה תקועה אחרי 60 שניות; (ה) דגל dirty "תקוע" באחסון מת לא חוסם יותר עדכון-עצמי ולא גורר דחיפה כל 15 שניות לנצח; (ו) התרעת "השמירה במכשיר נכשלה" דביקה ל-5 שניות — לא נדרסת ע"י "שומר…". נבדק שוב: סימולציות + קומפילציה + dev נקי.
- **נפרס לפרודקשן (27/07 ערב) באישור אורי.** האימון מהסרטון (שישי שבוע 2) לא קיים בשום מקום — צריך להזין אותו שוב אחרי הפריסה.

### ביקורת: הצעת תיקון בת 5 סעיפים ל"כתיבות שנכשלות בשקט" — נבדקה, לא יושמה (27/07, לילה)
**הרקע:** הועלתה הצעה לתקן "הכנסות (inserts) מספארי/PWA שנכשלות בשקט — השורות לא מגיעות לסופאבייס אבל הממשק מציג אותן בגלל עדכון אופטימי", בחשד ל-JWT שפג אחרי הקפאת הטאב או מחיקת localStorage ע"י ITP. ההצעה נבדקה מול הקוד בפועל (ביקורת רב-סוכנית) — **אף אחד מחמשת הסעיפים לא מתאים לארכיטקטורה של הפרויקט, וארבעה מהם היו גורמים נזק.** לא בוצע שום שינוי בקוד.

**למה זה לא מתאים:** אין בפרויקט אף קריאת `.insert()` (וגם לא `.update()`/`.delete()` דרך `from()`) — כל כתיבה היא `upsert` של **בלוב אחד בגודל ~70KB** לשורת `states` בודדת. `boot.js` לא נוגע ב-React בכלל; הוא עוטף את `localStorage.setItem`. אין "רשימה" לרענן, ו-localStorage הוא מקור האמת המקומי — רק **הדחיפה לשרת** בסיכון.

**הסעיפים והפסיקה:**
1. הוספת `.select().single()` לכל כתיבה — **מזיק**: `.select()` מחזיר את הבלוב (~70KB) בכל שמירה → תוספת egress של כ-2GB לחודש ב-100 משתמשים; `.single()` הופך כתיבות מוצלחות לשגיאת PGRST116 → חיווי אדום ולולאת ניסיונות אינסופית. בדיקת השגיאה כבר קיימת במסלול המרכזי.
2. הגדרת `auth:{...}` מפורשת — **מזיק**: שלושה מהערכים הם ברירת המחדל של הספרייה; `storage: window.localStorage` **אינו** — הוא מנטרל את בדיקת-הכתיבה של supabase-js ואת נפילת-החזרה לזיכרון, וקריאה ל-`window.localStorage` בשורה 22 זורקת `SecurityError` במצב "חסום עוגיות" באייפון → **האפליקציה לא עולה בכלל** (מסך פתיחה לבן לנצח).
3. `visibilitychange` → `refreshSession()` + הפניה ללוגין — **מזיק**: הספרייה כבר עושה זאת בעצמה, ו-boot.js כבר מטפל בשני הכיוונים. "הפניה בכשל רענון" זורקת החוצה משתמש בחדר כושר בלי קליטה — ומוחקת את `memTracker`, העותק היחיד של האימון כשה-localStorage מת (בדיוק המכשירים ש-1.7.1 נבנה בשבילם).
4. `getSession()` לפני כל כתיבה + חסימה — **מזיק**: supabase-js כבר עושה זאת לפני כל בקשה. חסימת כתיבה היא הדרך המהירה ביותר חזרה לבאג איבוד-הנתונים: בלי כתיבה אין חותמת `dirty` → באתחול הבא הקוד נופל לענף שמוחק את המכשיר.
5. רענון הרשימה אחרי insert — **הנחה שגויה**: אין insert ואין רשימה. `app.html` קורא את הטראקר פעם אחת ב-mount בלבד, ולכן רענון כזה בלתי נראה עד reload. המיזוג יום-אחרי-יום של 1.7.0 כבר עושה בדיוק את ההשוואה הזו — באתחול, במקום הנכון.

**מה כן נכשל בשקט (אומת בקריאת קוד):**
- `keepalivePush` — הערוץ **היחיד** שבאמת שקט: `.catch(function () {});` ריק, ל-`res.ok` אין ענף else, ואין `getSession`/רענון — הוא שולח טוקן מהמטמון. כאן החשד ל-JWT שפג **נכון**. בנוסף הגוף (~70KB) חורג ממגבלת ה-64KB של keepalive.
- פרסום `shared_program` של האדמין רץ **אחרי** שהחיווי כבר אמר "נשמר", והתוצאה לא נבדקת.
- `index.html` — ה-upsert של הפרופיל בהרשמה לא נבדק וההפניה מתבצעת בכל מקרה (חשבון בלי שורת פרופיל → המשתמש לא מופיע בלוח).
- למצב "סשן מת" אין יציאה: ה-`onAuthStateChange` היחיד רק שומר טוקן; `SIGNED_OUT` לא מטופל → חיווי אדום אינסופי בלי אפשרות להתחבר מחדש.
- ובאפליקציה עצמה: `app.html:1873` מקפיץ "🎉 האימון נשמר והושלם!" **ללא תנאי**, לפני כל פעולת רשת, ו-`save()` בולע כשל כתיבה. זו ה"הבטחה האופטימית" האמיתית — לא ה-insert.

**הסתייגות:** 7 מתוך 17 הסוכנים בביקורת נפלו על מגבלת סשן — שלב האימות הנגדי לא רץ. שלוש הטענות החמורות אומתו ידנית בקריאת קוד; השאר לא.

### v1.7.2 — הפסקת הכשלים השקטים (28/07)
אורי דיווח ש"שום דבר לא השתנה" אחרי 1.7.1, וביקש ליישם את התיקונים מהביקורת. **חשוב להבנה: התיקונים האלה חושפים כשלים, הם לא מתקנים סנכרון שבור בפני עצמם** — המטרה היא שכשל יפסיק להיות בלתי נראה.

**מדידה שהפכה השערה לעובדה:** נמדד בדפדפן שבלוב הטראקר הוא **71,288 בתים**, וגוף דחיפת ה-keepalive יוצא **71,399 בתים** — מעל מגבלת ה-64KB הקשיחה של הדפדפן. כלומר **דחיפת הסגירה נכשלה בכל סגירה, אצל כל משתמש**, מאז שהתוכנית גדלה. זה לא היה ניתן לראות כי הדחייה נבלעה ב-`.catch` ריק.

**מה תוקן:**
1. **`keepalivePush` הפסיק להיות שקט** — בדיקת גודל מול `KEEPALIVE_MAX` (60KB) לפני השליחה: אם הגוף חורג, לא שורפים בקשה שנועדה להיכשל אלא רושמים `console.warn` ומסתמכים על חותמת ה-dirty. נוסף ענף לתשובה לא-תקינה (`console.warn` + סיווג 401/403 כ-`auth`), ו-`.catch` שמדווח במקום לבלוע. הסיבה נשמרת במפתח `cfby_ka_fail_v1` ונקראת באתחול הבא (`console.warn` בלבד — המיזוג כבר משחזר את הנתונים, אסור להבהיל).
2. **חיווי `reauth` למצב סשן מת** — `onAuthStateChange` מטפל עכשיו ב-`SIGNED_OUT` שלא יזמנו, ו-`doPushState` מסווג שגיאת אימות (401/PGRST301/jwt) כשהרשת חיה. **בלי הפניה אוטומטית ובלי חסימת כתיבה** — החיווי הוא היחיד שניתן ללחיצה, והלחיצה מנסה דחיפה אחרונה ומזהירה לפני שמוותרים על נתונים לא מסונכרנים. הניסיונות החוזרים ממשיכים ברקע (60 שניות במקום 10) כדי שסשן שמתאושש לבד יסתנכרן בלי מגע יד.
3. **`shared_program`** — התוצאה נבדקת, והפרסום הועבר **לפני** חיווי ה"נשמר". חיווי חדש `pubfail`: "האימון נשמר, אך פרסום התוכנית נכשל" (רלוונטי לאדמין בלבד).
4. **`signOut` עם `scope:"local"`** — ברירת המחדל היא `global`, כלומר יציאה בטלפון ביטלה את הסשן בכל שאר המכשירים. זה עצמו ייצר סשנים מתים.
5. **`index.html`** — ה-upsert של הפרופיל בהרשמה נבדק, עם ניסיון חוזר אחד. **לא חוסם את ההפניה** — החשבון כבר קיים בשלב הזה, והחזרה למסך ההרשמה רק הייתה מקבלת "שם המשתמש כבר תפוס".
6. **הטוסט ב-`app.html`** — "🎉 האימון נשמר והושלם!" הפך ל-"🎉 האימון הושלם ונשמר במכשיר". הטוסט רץ לפני כל פעולת רשת ולכן לא יכול לדעת על הענן; ההבטחה על הענן שייכת רק לחיווי של boot.js.
- אומת בדפדפן: האפליקציה עולה נקי ב-dev בלי שגיאות קונסול, כל 7 מצבי החיווי מרונדרים נכון, ו-`reauth` הוא היחיד עם `pointer-events:auto` (ומאבד אותו במעבר למצב אחר — בלי אזור נגיעה תקוע).
- **מה שעדיין פתוח:** ערוץ הסגירה עצמו עדיין מת בפועל (הבלוב גדול מהמגבלה) — עכשיו זה גלוי במקום שקט. הפתרון האמיתי הוא RPC שמקבל רק ימים שהשתנו; דורש SQL ולא נכלל כאן.

### v1.7.3 — ערוץ הסגירה חוזר לחיים: RPC של ימים-בלבד (30/07)
אורי דיווח: "משהו לא עובד לי בשמירת אימונים דרך האייפון". בדיקת התשתית: פרודקשן מגיש 1.7.2 כמו שצריך והקוד ללא רגרסיה — אבל **החור שנשאר פתוח ב-1.7.2 הוא בדיוק תרחיש האייפון**: רושמים אימון וסוגרים מיד (לפני שה-debounce של 800ms יורה), דחיפת הסגירה מדלגת (הבלוב 71KB מעל תקרת ה-64KB של הדפדפן) — ואם גם האחסון המקומי מת בשקט (המצב המתועד מהאייפון של אורי ב-27/07), האימון אבד לתמיד. זה תרחיש הסרטון מ-27/07 אחד-לאחד.

**התיקון — הרעיון שחיכה ביומן ("RPC שמקבל רק ימים שהשתנו") נבנה:**
1. **פונקציית `push_days(p_days, p_dts)` במסד** (קובץ חדש `supabase/push_days.sql`; נוספה גם ל-schema.sql הקנוני): מקבלת רק את הימים שנכתבו בסשן (~2KB במקום 71KB — עמוק מתחת לתקרה) וממזגת אותם לתוך בלוב ה-states של המשתמש המחובר בלבד, יום-אחר-יום לפי חותמות "נגיעה אחרונה" (tracker._dts) — אותו כלל כמו מיזוג האתחול של v1.7.0, כך שמכשיר עם עותק ישן לא יכול לדרוס יום חדש גם דרך הערוץ הזה. SECURITY INVOKER — ה-RLS הרגיל חל במלואו. כשאין שורת states היא לא יוצרת אחת ("שלד ריק" לעולם לא ינצח נתונים אמיתיים — הדחיפה המלאה הבאה תיצור את השורה).
2. **boot.js — `memDirtyDays`**: מראה חדשה בזיכרון של מפתחות הימים ("w_d") שנכתבו מאז הדחיפה המלאה המאושרת האחרונה. חיה בזיכרון כמו memTracker — כלומר שורדת localStorage מת. `stampChangedDays` מחזירה עכשיו את מפתחות הימים שהשתנו, והאינטרספטור אוסף אותם.
3. **`keepalivePush` הפך ל-RPC-first**: קודם מנסה לשלוח רק את הימים המלוכלכים ל-`/rpc/push_days` (keepalive, גוף זעיר); נפילה חיננית למסלול הבלוב המלא הישן כשאין מפתחות ימים (למשל עריכת תוכנית של אדמין — היא בכוונה לא מייצרת חותמות יום) או כשהמבנה המקומי חסר. חותמות ה-dirty **לא מנוקות** בהצלחת ה-RPC: הדף גוסס ו-`.then` לא מובטח שירוץ, הפתיחה הבאה ממילא עושה דחיפה מלאה ומיישרת, ושליחה חוזרת של יום שכבר הוחל היא אידמפוטנטית.

- אומת: 9 בדיקות על הקוד האמיתי (py-mini-racer, ה-harness מהסימולציות של 27/07) — כולל התרחיש הקריטי "אחסון מת לגמרי: האימון עדיין יוצא מהמראות בזיכרון אל ה-RPC" (גוף נמדד: 289B מול 71,399B שנכשל); `push_days.sql` ו-schema.sql עוברים פרסר PostgreSQL אמיתי (pglast); dev עולה נקי ללא שגיאות קונסול.
- **מה זה סוגר:** רישום אימון → סגירה מיידית → האימון בענן, גם כשהאחסון במכשיר מת. הפתיחה הבאה משחזרת אותו מהשרת דרך המיזוג.
- **⚠️ עד שה-SQL מורץ (ראו למטה):** הערוץ מתנהג בדיוק כמו ב-1.7.2 — מדלג ורושם `norpc` ל-`cfby_ka_fail_v1`. אין רגרסיה; פשוט אין עדיין את רשת הביטחון החדשה.

### v1.7.4 — "אין לי חיווי" + "לא כמו במחשב": שלושת הממצאים מהנייד של אורי (01/08)
אורי הריץ את ה-SQL של push_days (אומת מבחוץ: הפונקציה קיימת במסד — בדיקה אנונימית מחזירה "permission denied", כלומר מותקנת ומוגנת כמתוכנן), אבל דיווח: עדיין "לא שומר", אין שום חיווי סנכרון בנייד, וכתוב למטה "הנתונים נשמרים אוטומטית במכשיר זה". שלושה ממצאים, שלושה תיקונים:

1. **החיווי נמחק מה-DOM בכל רינדור — באג אמיתי.** הרנטיים של האפליקציה בונה מחדש את `<body>` בכל רינדור (בדיוק הסיבה שתגית הגרסה צריכה MutationObserver מאז 1.6.15) — אבל לגלולת הסנכרון לא הייתה הגנה כזאת. כל שמירה גוררת סדרת רינדורים מיידית (טוסט נכנס/יוצא, היום מתהפך ל-✓) שזורקת את הגלולה החוצה בדיוק כשהיא מוצגת; בנייד גם נועלים את הטלפון מיד, אז אף פעם לא תופסים אותה חיה. במחשב התזמונים שונים ולכן שם היא כן נראתה. **תוקן:** הגלולה מוחזקת ברפרנס, וה-observer של תגית הגרסה מחזיר גם אותה אחרי כל rebuild (המצב נשמר). הוגבהה ל-72px גם ב-PWA כדי שהטוסט של האפליקציה לא יסתיר אותה.
2. **הטקסט "הנתונים נשמרים אוטומטית במכשיר הזה" — שקר סטטי מהאפליקציה המיוצאת** (app.html, היה שם מאז ומעולם; שרד את כל העטיפה). אורי קרא אותו והסיק — בצדק — שאין סנכרון ענן. **תוקן:** הטקסט הוחלף ("הנתונים מסתנכרנים לענן אוטומטית"), וגם הטוסט ("ונשמר במכשיר" → "ונשמר — מסתנכרן לענן…"), וגם ההערה במודל השמות. ובעיקר: **שורת מצב-ענן קבועה בפוטר**, לצד הגרסה, ששורדת רינדורים דרך אותו observer: "☁️ מסונכרן לענן · 14:32" / "☁️ שומר בענן…" / "⚠️ לא מסונכרן — מנסה שוב" / "📡 אין אינטרנט" / "🔑 ההתחברות פגה — הקישו כאן" (לחיץ, עם אישור לפני נטישת נתונים לא מסונכרנים). זו תשובה קבועה לשאלה "הגיע לענן?" — לא גלולה של 3 שניות.
3. **"רוצה לראות את הנתונים בכמה מכשירים בעדכון מהיר" — פער אמיתי בתכנון.** ההתאמה בין מכשירים קרתה רק ב-boot: מחשב שנשאר פתוח לא מושך אף פעם, אז אימון מהנייד "לא נשמר" מבחינת מי שמסתכל על המחשב. **נבנה `checkCloudFresh`:** בכל חזרה לעמוד נקי (visible), שאילתת `updated_at` בלבד על שורת ה-states של המשתמש (בייטים בודדים — לא הבלוב, לא פוגע ב-egress); אם ההעתק בשרת חדש יותר מזה שהעמוד מכיר — reload, ומיזוג הימים של ה-boot מביא את הנתונים. רק מעל עמוד נקי (שרת לעולם לא מנצח עבודה לא מסונכרנת — האינווריאנט נשמר), עם סף 1.5 שניות, ריסון 30 שניות, וכשל/אין-שורה לא עושים כלום (סשן מת נראה כמו ריק תחת RLS).

- אומת: 8 בדיקות חדשות על הקוד האמיתי (כולל סימולציית "rebuild של body" שמוכיחה שהגלולה והפוטר שורדים) + 9 בדיקות הרגרסיה של 1.7.3 — כולן עוברות; dev נקי.
- **מה עדיין לא ידוע:** האם הדחיפות מהאייפון של אורי באמת נכשלות, או שהכול נשמר ורק הנראוּת שיקרה. מעכשיו הפוטר עונה על זה בעצמו: אם אחרי אימון כתוב "מסונכרן לענן · שעה" — הכול עבד תמיד; אם כתוב 🔑/⚠️ — זו התשובה ונטפל בה נקודתית.

### אימוני שבוע 4 — השבוע המלא, ראשון 2/8 עד שבת 8/8 (03/08, 10/08) — v1.7.5
אורי שלח שני צילומים והנחה: שניהם ליום ראשון 2/8 — הארוך (יום מלא) הוא ה-WOD הרגיל, הקצר (ריצות) הוא האינדורנס.
- **נוצר `programWeek4()`** (שבוע 4 באפליקציה = 2-8/08; הוטמע יום ראשון בלבד):
  - **Lift:** Clean & Jerk — Heavy Single Day (Every 2:00 x 7: סטים 1-2 קומפלקס @75%, סטים 3-7 C&J בעלייה 80→85→90%).
  - **Metcon:** אינטרוולים 1:30 עבודה / 1:00 מנוחה x 4 — 20 ניתורים מעל המוט, 10 פרונט סקווט, ואז מקס פאוור קלינים בזמן שנותר. רמות 35/25 · 40/30 · 50/35 ק"ג. **ניקוד: סה"כ קלינים** (`resultMode:'amount'` — כמות מסוכמת, לפי כלל הניקוד הקבוע; כותרת "For Time" שבצילום לא תורגמה למצב זמן כי הציון המוצהר הוא ספירת קלינים).
  - **Extra:** Conditioning — Mono Session "The Pace Turn" (ריצות 800+400 / 500+1000 לפי רמה, עם המרות חתירה/סקי/אופניים).
  - **אינדורנס (alt):** 3 x 1600m ריצה, מנוחה 3-4 דק' — ניקוד זמן = סכום שלוש הריצות (אותה תבנית כמו METCON-B של שבוע 3). הנעילה ההדדית של ימי ראשון (סשן אחד נספר) חלה אוטומטית — אומת ש-`dayHasPlan(alt)` נדלק.
- `PROGRAM_VERSION` 13 → 14 (מתעדכן אצל כולם בפתיחה הבאה בלי לגעת בלוגים); שלישיית הגרסה עודכנה ל-**1.7.5** ונפרס.
- **אומת מול הפרודקשן החי:** ה-HTML שהאתר מגיש נמשך, הקלאס המוטמע הורץ כלשונו ונבנתה התוכנית — שבוע 4 יום 0 נופל בדיוק על Sun Aug 02 2026, כל שדות ה-WOD והאינדורנס במקומם, שאר ימי שבוע 4 ריקים, ושבועות 1-3 לא נפגעו.

**יום שני 3/8 (הוטמע בהמשך אותו סשן):**
- **Lift:** Snatch — Heavy Single Day (Every 2:00 x 9: סטים 1-2 Snatch + Below knee hang squat snatch @70-75%, סטים 3-9 סינגל בעלייה 80→83→86→89%, יעד סיום 2 סינגלים ±90%).
- **Metcon:** AMRAP 10:00 — 15 וולבול, 10 ברפי לטרלי, 5 פאוור סנאץ'. רמות 40/30 ומטה · 50/35 · 60/40 ק"ג. **ניקוד: סבבים+חזרות** (`resultMode:'rounds'` לפי הכלל הקבוע לכל AMRAP; בצילום נכתב "total reps" — נוסף בהערה "סבב = 30 חזרות" כך שההמרה חד-חד-ערכית. אם אורי מעדיף ספירת חזרות גולמית — לשנות לפני שנרשמות תוצאות).
- **Extras (שניים):** Alternating EMOM 9:00 (DB high pulls / plate reverse fly / Copenhagen plank) + "3 Rounds — No Rush" (good mornings + side hip abductions).
- **הנחיית גרסה חדשה מאורי (03/08): לא נוגעים במספר הגרסה בכלל במהלך הטמעות שבוע 4** — BUILD/version.json נשארים 1.7.5; רק `PROGRAM_VERSION` מוקפץ (14→15, כמו בתקדים מ-27/07 — בלעדיו מכשירים קיימים לא מקבלים את היום החדש). כשתסתיים הטמעת כל השבוע — אורי יחליט את מספר הגרסה.
- אומת מול הפרודקשן: יום שני נופל על Mon Aug 03 2026 עם כל השדות; ראשון נשאר שלם; version.json בפרודקשן עדיין 1.7.5.

**יום שלישי 4/8 (הוטמע 10/08):**
- **Lift:** Push Press + 2 Push Jerks — Every 2:00 x 5 סטים (2 @75%, 2 @78%, 1 @81%+); "Convert the push press into a good push jerk".
- **Metcon:** 3 Rounds For Time עם מנוחה 1:30 בין סבבים — Double unders, סט C2B, Burpee box jump overs, סט C2B נוסף. רמות: L1 = 20 DU / 20s תרגול / 40 SU + 8 C2B + 8 BBJO · L2 = 30 DU + 10 + 10 · L3 = 50 DU + 14 + 14. **ניקוד: זמן כולל** (`resultMode:'time'` — For Time, לא AMRAP, לפי הכלל הקבוע). ה-BBJO חייבים לפנות אל הארגז. בסבב יש 5 תחנות אבל רק 4 משבצות תנועה באפליקציה — המנוחה עברה לשורת הסכימה ("· Rest 1:30") ולהערה.
- **Extra:** Strength — Front Squat מהמתקן (Every 2:00 x 6 סטים, 2 חזרות בעלייה; התחלה ±70%, סיום ±8.5RPE) + משבצת שנייה ריקה מפורשת (`['','']`, כלל ה-applyProgram).
- ל"בריף היומי" שבצילום אין שדה באפליקציה — כמו בשבועות הקודמים הוא משמש להקשר בלבד ולא הוטמע.
- `PROGRAM_VERSION` 15 → 16; BUILD/version.json נשארים **1.7.5** (הנחיית ההקפאה של אורי בתוקף — אישר אותה שוב בתחילת הסשן).
- אומת ב-dev מקומי (localhost + `?dev`): שלישי נופל על Tue Aug 04 2026 · WEEK 4 עם כל השדות (זמן כשדה תוצאה, RX/Scaled, אקסטרה אחת מוצגת והשנייה נסתרת), נקודות "יש תוכנית" דולקות רק על ראשון-שלישי, אפס שגיאות קונסול; שבועות 1-3 לא נפגעו.

**יום רביעי 5/8 (הוטמע 10/08):** לפני כן — אורי שלח בטעות שוב את צילום שלישי עם הכיתוב "רביעי 5.8"; זוהה כזהה (אותו בריף/כוח/מטקון/אקסטרה), לא הוטמע, והצילום הנכון הגיע בהודעה הבאה.
- **Lift:** Strength — Carries + Single Arm DB Rows — Every 1:30 for 8 sets, alternate: ‎30m Dball carry / Double KB front rack carry ↔ ‎8+8 Single arm DB bent rows.
- **Metcon:** Every 3:00 for 5 sets — ‎400m Run + KB swings (Russian). רמות: L1 = 300m + 10 סווינגים · L2 = 15 · L3 = 20. **ניקוד: הסבב האיטי ביותר** (`resultMode:'time'`, כמו ה-E4MOM של שבוע 2). ההערה בעברית על 30 שניות נשימה בסוף סבב נכנסה (תוקן "שנית"→"שניות").
- **Extra:** 3 Rounds — No Rush (10-15 GHD sit-ups · 10 Hip extensions · 20s L-hang) + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 16 → 17; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: רביעי נופל על Wed Aug 05 2026 · WEEK 4 עם כל השדות; מסלול השדרוג pv 16→17 נבדק בפועל (פרופיל dev עם pv=16 קיבל את היום החדש אחרי רענון); אפס שגיאות קונסול.

**יום חמישי 6/8 (הוטמע 10/08):** "Big Tuesday. בחמישי." — יום עם שני ניקודים, הוטמע במבנה metcon+metcon2 (התקדים של Big Tuesday משבוע 2).
- **Lift:** Back Squat — Every 3:00 x 6 סטים (1-2: ‏3 @83% · 3-4: ‏2-3 @85-86% · 5-6: ‏2-3 @87-88%), אחוזים מ-CapaciTest #1; "Heaviest squats of the block" + חלופת RPE בעברית למי שלא ביצע סקוואטים בבלוק.
- **Metcon (חלק 1):** Alternating EMOM 12:00 ‏(0:00-12:00) — Cal row / Double DB hang snatches / Box jumps ‎@24/20" step down. רמות דאמבלים: 2x12.5/7.5 · 2x15/10 · 2x17.5/12+. **ניקוד 1: ממוצע חזרות לדקה** (`resultMode:'amount'`). גשר "Rest 2:00, into Part 2" בהערה.
- **Metcon B (חלק 2):** AMRAP 10:00 בזוגות ‏(14:00-24:00) — ‏10 Synced burpees + ‏10 Cal row. **ניקוד 2: סה"כ קלוריות לזוג** (`resultMode:'amount'` — כלל הזוגות עם ניקוד מסוכם). ההערה: ברפיז ביחד, קלוריות מתחלקות חופשי.
- **Extra:** Capacity Pull Up Builder — Every 45s x 8 סטים, אותו גודל סט כמו שבוע שעבר, הפעם לכל 8 הסטים + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 17 → 18; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: חמישי נופל על Thu Aug 06 2026 · WEEK 4, שני המטקונים מרונדרים עם שדות כמות נפרדים, ושער השמירה דורש את שתי התוצאות ("תוצאה למטקון · תוצאה למטקון השני"); אפס שגיאות קונסול.

**יום שישי 7/8 (הוטמע 10/08):** "סיום טכני מאוד לשבוע".
- **Lift:** Power Snatch — Every 1:00 x 10 סטים, סינגל בכל דקה: 1-5 @70%, ‏6-10 בנייה בתחושה לסינגל פאוור כבד. "Confident feet, no misses" + ההנחיה בעברית למשוך לאט מהרצפה ולהאיץ.
- **Metcon:** Conditioning — Every 6:00 x 3 סטים: ריצת 800m ואז מקס T2B בזמן שנותר; **כל שבירה בסט ה-T2B = ‏2 וול-ווקים**. רמות: L1 = ‏400-600m + knee raises + וול-ווק אחד לשבירה · L2-3 כמו שכתוב. **ניקוד: סה"כ T2B** (`resultMode:'amount'`).
- **Extra:** BY Extra — Every 1:15 x 8: ‏20m Single arm KB front rack carry ואז מקס לחיצות כתף ביד אחת; "Set per arm!" והמרה לפוש פרס אם הלחיצה מגבילה + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 18 → 19; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: שישי נופל על Fri Aug 07 2026 · WEEK 4 עם כל השדות (שדה כמות לניקוד), 6 נקודות תוכנית (ראשון-שישי); אפס שגיאות קונסול.

**יום שבת 8/8 (הוטמע 10/08) — שבוע 4 הושלם:** "שבת קבוצות 😎".
- **Lift:** Silent Deadlifts + Holds — Every 1:30 x 8 סטים לסירוגין: ‏5 דדליפטים שקטים ↔ מקס L-hang + ‏30s hollow hold. "Heavy but clean. Control the deadlift down."
- **Metcon:** In Teams of 3 — AMRAP 20:00 (שניים עובדים, אחד נח): ‏20 Synced burpees over the DB · ‏20 Synced DB snatches · ריצת 200m שלושתם יחד · ‏20 HRPU. **ניקוד: סבבים+חזרות** (`resultMode:'rounds'` לפי כלל ה-AMRAP הקבוע — אין בצילום ניקוד מסוכם, אז חריג הזוגות לא חל). ההערה בעברית על החלפת תפקידים ותיאום ציפיות נכנסה.
- **Extra:** BY Extra — ‏4 sets for quality: ‏1-2 Legless rope climbs · ‏10-15 Banded face pulls · ‏60s rest + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 19 → 20; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: שבת נופלת על Sat Aug 08 2026 · WEEK 4, שדות סבבים+חזרות פעילים, כל 7 ימי שבוע 4 עם נקודת תוכנית; אפס שגיאות קונסול. **כל ארבעת השבועות הראשונים מלאים (7/7/7/7).**
- **צ'קפוינט גרסה:** לפי הנחיית אורי מ-03/08 — עכשיו, כששבוע 4 הושלם, אורי בוחר את מספר הגרסה. עד שיגיד — הכול נשאר 1.7.5.

### שבוע 5 נפתח — ראשון 9/8 (הוטמע 10/08) — v1.7.5
לפני כן — אורי שלח שוב בטעות את צילום שבת עם הכיתוב "ראשון 9.8 WOD"; זוהה כזהה (הבריף "שבת קבוצות"), לא הוטמע, והצילום האמיתי הגיע אחריו. **נוצר `programWeek5()` + שורת `overlay(4, ...)` ב-applyProgram** (שבוע 5 באפליקציה = 9-15/08).
- **Lift:** Back Squat — Every 3:00 x 5 סטים (1-2: ‏3 @80% · 3-4: ‏2 @85% · 5: ‏2 @88%+), אחוזים מ-CapaciTest #1. "המוט זז מהר או שאתם מפסיקים — לא גריינדים בשבוע כזה" + חלופת RPE למי שלא בדק שלשה (2 @7.5RPE, ‏2 @8.5, ‏1 @9).
- **Metcon:** Every 2:00 for 12:00 (6 סבבים) — Cal row לפי רמה (9/6 · 11/8 · 14/10) → ‏10 KB swings (אמריקאיים, עד למעלה) → מקס DU בזמן שנותר. **ניקוד: סה"כ DU** (`resultMode:'amount'`). הזנקות במרווחי דקה אפשריות; היעד — לפחות חצי דקה על החבל.
- **Extras (שניים, מהצילום עצמו):** ‏Lift — Hang Power Snatch + 2 OHS (Every 1:30 x 6: ‏1-3 @60%, ‏4-6 @65-70% בתחושה; "רק מכינים את הגוף — שומרים את הראש והרגליים לסקוואט") + Successory — 3 Rounds No Rush (8 good mornings, ‏10-15 side hip abduction; "Same pair — a touch heavier on the GMs").
- `PROGRAM_VERSION` 20 → 21; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: ראשון נופל על Sun Aug 09 2026 · WEEK 5 עם כל השדות (שדה כמות, שתי אקסטרות); שבועות 1-4 נשארו 7/7/7/7; אפס שגיאות קונסול.

**אינדורנס לראשון 9/8 (הוטמע בהמשך, צילום נפרד):** נוסף `alt` ליום — כמו במבנה של שבועות 1-4:
- **Metcon (alt):** AMRAP 18:00 — ‏800m Row/Run + ‏15 Wall balls. **ניקוד: סבבים+חזרות** (`resultMode:'rounds'`, כלל ה-AMRAP).
- **METCON-B (alt):** Every 2:00 for 12:00 — ‏8-12 Cal row → ‏10 KB swings → מקס DU בזמן שנותר. **ניקוד: סה"כ DU** (`resultMode:'amount'`) — גרסת האינדורנס של המטקון מה-WOD הראשי (שם הקלוריות לפי רמות).
- אקסטרות alt ריקות מפורשות; הנעילה ההדדית של ימי ראשון (סשן אחד נרשם) חלה אוטומטית דרך `dayHasPlan(alt)`.
- `PROGRAM_VERSION` 21 → 22; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: טאב "🚴 ENDURANCE" מופיע ביום ראשון של שבוע 5, ובלחיצה מרונדרים שני המטקונים של האינדורנס עם שדות הניקוד הנכונים (סבבים+חזרות / כמות); ה-WOD הראשי נשאר שלם; אפס שגיאות קונסול.

**יום שני 10/8 (הוטמע 10/08 — היום עצמו):**
- **Lift:** Clean Pull + Squat Clean + Jerk — Every 2:00 x 6 סטים: ‏3 @72%, ‏3 @76-82%; או לחילופין 3 סטים במאמץ 7/10 ועוד 3 במאמץ 8/10 (בעברית, כמו בצילום). שם התרגיל נבחר כשם הקומפלקס (ולא "Clean & Jerk Complex" של שבוע 2) כדי לא לערבב היסטוריות PR של קומפלקסים שונים.
- **Metcon:** Mono Session — AMRAP 22:00 בזוגות: פרטנר 1 רץ (400/600/800m לפי רמה — "Partners can be on different levels"), פרטנר 2 על מכונה (Bike/Row/Ski) או ברפי בלי שכיבת סמיכה. **ניקוד: סבבים+חזרות** (`resultMode:'rounds'`, כלל ה-AMRAP — אין ניקוד מסוכם בצילום). ההערה המלאה בעברית נכנסה, כולל היעד "כ-4 סבבים".
- **Extra:** Alternating EMOM 9:00 (‏8+8 DB high pulls / ‏10 plate reverse fly / ‏30s Copenhagen plank לכל צד) — אותה אקסטרה כמו שני של שבוע 4 + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 22 → 23; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: "פתח את האימון של היום" נופל ישר על Mon Aug 10 2026 · WEEK 5 עם כל השדות (סבבים+חזרות, שתי משבצות הפרטנרים); יום ראשון + האינדורנס שלו נשארו שלמים; אפס שגיאות קונסול.

**יום שלישי 11/8 (הוטמע 10/08, יום מראש):**
- **Lift:** Deadlifts — Every 2:00 x 5 סטים, ‏3 חזרות @70%; "Reset each rep, lower quietly to the floor" + "ירידה בשליטה — לא מעירים את השכנים". השם 'Deadlifts' תואם לשבוע 2 כדי לאחד היסטוריית משקלים.
- **Metcon:** Big Tuesday — Alternating EMOM 18:00: דקה 1 ברפי לטרלי מעל קו · דקה 2 ‏8-10 פאוור קלינים + מקס T2B · דקה 3 ‏3-5 שאטלים (8+8m, מנוחה אקטיבית). רמות: 6-8 ברפיז + ‏8 קלינים @40/30 · ‏10 @45/32.5 · ‏12-14 @50/35. **ניקוד: סה"כ T2B** (`resultMode:'amount'`). ההערה המלאה בעברית נכנסה (גריפ, סטים גדולים).
- **Extras (שניים):** Conditioning — Every 2:00 x 6 (BBJO ‏8/10/12 לפי רמה + מקס קלוריות אופניים בזמן שנותר) + Successory — Capacity Pull Up Builder (Every 60s x 6-7, לשמור מספר; מי שעשה את הבילדר מוסיף חזרות).
- `PROGRAM_VERSION` 23 → 24; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: שלישי נופל על Tue Aug 11 2026 · WEEK 5 עם כל התוכן; שדה התוצאה מוצג נעול ("🔒 הזנת התוצאות תיפתח ביום האימון") כי היום עתידי — התנהגות תקינה; אפס שגיאות קונסול.

**יום רביעי 12/8 (הוטמע 10/08, יומיים מראש):**
- **Lift:** Strength — Strict Press + Reverse Lunges — Every 1:30 x 10 סטים לסירוגין: ‏6 לחיצות סטריקט ↔ ‏12 לאנג'ים אחוריים עם מוט (12 סה"כ). "עבודת כוח נקייה — לא כבד מקסימלי, אבל איכותי ומאתגר."
- **Metcon:** Aerobic — Every 3:00 x 5 סטים: ‏15 Goblet squats → שכיבות סמיכה → קפיצות ארגז @24/20" (עם ירידה בצעד). רמות: L1 = ‏10 על הברכיים + ‏10 קפיצות · L2 = ‏10-15 + ‏15 · L3 = ‏20 + ‏15. **ניקוד: הסבב האיטי ביותר** (`resultMode:'time'`).
- **Extra:** BY Extra — 3 sets for quality: ‏10 דיפים במשקל גוף · ‏30s hollow hold · ‏30s גרירת מזחלת אחורה (או פלאנק) + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 24 → 25; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: רביעי נופל על Wed Aug 12 2026 · WEEK 5 עם כל התוכן, הזנה נעולה כמצופה ליום עתידי; אפס שגיאות קונסול.

**יום חמישי 13/8 (הוטמע 10/08, מראש):**
- **Lift:** Snatch — Sharp Session — Every 1:30 x 7 סטים: ‏3 סטים Snatch with pause at knee + Snatch ‏@70%, ‏4 סטים סינגל @78-82%. "לא טיפוס למקסימום — מחזיקים 78-82% וגורמים לכל חזרה להיראות קלה. הסטים הראשונים מלטשים."
- **Metcon:** Gymnastics Build — ‏2:00 on / 1:00 off x 4: וולבול → ברפי למטרה → מקס C2B/מתח בזמן שנותר. רמות: 10/10 · 12/12 · 15/15. **ניקוד: סה"כ C2B/מתח** (`resultMode:'amount'`). "מגיעים למתח טיפה מותשים, ושם מתחילים למדוד ביצועים."
- **Extras (שניים):** Floating Snatch Pulls (Every 1:30 x 4, ‏2 חזרות @95-105% מהסנאץ') + Then — 3 Sets (10-15 Banded tricep extension · ‏30s Reverse sled drag).
- `PROGRAM_VERSION` 25 → 26; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: חמישי נופל על Thu Aug 13 2026 · WEEK 5 עם כל התוכן, הזנה נעולה כמצופה; אפס שגיאות קונסול.

**יום שישי 14/8 (הוטמע 10/08, מראש):** "יום שישי של פרונט ראק!"
- **Lift:** Front Squat — From the Rig — Every 2:00 x 6 סטים, ‏2-3 חזרות: ‏3 סטים @±75% ו-3 סטים @80%+ מהפרונט סקוואט. "בונים לאורך הסטים. עקבים על הרצפה, מרפקים גבוה" + חלופת 7-8RPE למי שלא בדק.
- **Metcon:** 5 Sets For Time — ‏DU (לפי רמה) + ‏5 Jerks, מנוחה 30s בין סטים. רמות: 20 DU @40/30 · ‏30 DU @60/40 · ‏50 DU @80/50. **ניקוד: זמן כולל** (`resultMode:'time'`). ההערה המלאה בעברית נכנסה ("כמעט עבודת כוח").
- **Extra:** Conditioning — Mono Session "Ceiling": ‏L1-2 — 5 סטים של 500m ריצה / 500/400m חתירה/סקי / 30/22 קל' אופניים, מנוחה 1:00; ‏L3 — 5-6 סטים של 800m / 800/650m / 45/33 קל'. Score — ממוצע זמן לסט; Goal — מהר מקצב הטסט + משבצת שנייה ריקה מפורשת.
- `PROGRAM_VERSION` 26 → 27; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: שישי נופל על Fri Aug 14 2026 · WEEK 5 עם כל התוכן, הזנה נעולה כמצופה; אפס שגיאות קונסול.

**יום שבת 15/8 (הוטמע 10/08, מראש) — שבוע 5 הושלם:** "שבת זוגות צבעונית :)".
- **Skill (חדש בצילומים):** ‏Practice 15:00 — Handstand hold / walk work. אין שדה סקיל באפליקציה — הוכנס כ-**Extra #1** ("Skill — Practice 15:00"). נשקל metcon2 ונפסל: שער השמירה היה דורש "תוצאה" לסקיל שאין לו ניקוד; אקסטרות לא חוסמות שמירה.
- **Lift:** Upper Body Strength — DB Bench + Rows (אותו שם כמו שבת שבוע 2 — היסטוריה מאוחדת) — Every 1:30 x 8 לסירוגין: ‏10 לחיצות DB ↔ ‏8+8 חתירה ביד אחת. "Heavy but clean on both" + "עבודה משלימה לגוף העליון".
- **Metcon + Metcon B:** In Pairs, I-go-you-go all the way — ‏AMRAP 10:00 ‏#1 (20 Knees to elbows · ‏20 Hang DB snatches "grab a bit heavy DB" · ‏20 ברפי לטרלי מעל ה-DB) ישר לתוך ‏AMRAP 10:00 ‏#2 (‏20 V-Ups · ‏20 Hang DB C&J · ‏20 Jumping lunges במשקל גוף). **ניקוד: סבבים+חזרות לכל חלק** (`resultMode:'rounds'` בשניהם, כלל ה-AMRAP; "מחליפים תפקידים מתי שרוצים" בהערות).
- **Extra #2:** BY Extra — 4 sets for quality (1-2 Legless rope climbs · ‏10-15 Banded face pulls · ‏60s rest) — זהה לשבת שבוע 4.
- `PROGRAM_VERSION` 27 → 28; BUILD/version.json נשארים 1.7.5.
- אומת ב-dev: שבת נופלת על Sat Aug 15 2026 · WEEK 5, שני המטקונים מרונדרים (METCON + METCON B), אקסטרת הסקיל מוצגת, כל 7 ימי שבוע 5 עם נקודת תוכנית; אפס שגיאות קונסול. **חמשת השבועות הראשונים מלאים (7/7/7/7/7).**

### v1.8.0 — אורי שחרר את הקפאת הגרסה (10/08)
בסיום הטמעת שבועות 4+5 אורי הנחה: "תדחוף גרסא 1.8.0". שלישיית הגרסה עודכנה — `BUILD` ב-boot.js ו-`public/version.json` הועלו 1.7.5 → **1.8.0** — ונפרס. אין שינויי קוד נוספים בגרסה הזו; היא חותמת את 13 ימי התוכנית שהוטמעו (PROGRAM_VERSION ‏15→28: שלישי-שבת של שבוע 4, שבוע 5 מלא כולל אינדורנס, ואקסטרת סקיל ראשונה).
- אומת ב-dev: version.json ו-boot.js המוגשים מחזירים 1.8.0, והפוטר מציג "גרסה 1.8.0" אחרי רענון (בלוקאל נדרש ניקוי מטמון ידני כי python http.server לא שולח כותרות cache — בפרודקשן Vercel + בדיקת העדכון העצמי של boot.js מול version.json (no-store) מטפלים בזה אוטומטית); אפס שגיאות קונסול.
- מכשירים קיימים: יראו את התוכניות החדשות מיד (PROGRAM_VERSION), ואת תג "גרסה 1.8.0" ברגע שבדיקת העדכון העצמי תרוץ אצלם.

**"הגרסא לא נדחפה" (10/08, 20:17) — ממצא: כתובת דיפלוי קפואה.** אורי ראה "גרסה 1.7.5" בדפדפן — אבל הכתובת בטאב שלו הייתה `crossfit-bat-yam-capacity-68z7tjsh7-oriuz.vercel.app` — **כתובת של דיפלוי ספציפי** (עם hash), שורסל מקפיא לנצח על אותו בילד. בכתובת כזו גם העדכון העצמי לא יעזור לעולם: ה-version.json שהיא מגישה קפוא גם הוא על 1.7.5, אז ההשוואה תמיד שווה. אומת מבחוץ שהפרודקשן הקנוני בסדר גמור: `crossfit-bat-yam-capacity.vercel.app` מגיש version.json = **1.8.0** ו-app עם **PROGRAM_VERSION = 28** כולל programWeek5. זה כנראה גם ההסבר לתלונות "bundle ישן" מהעבר (הפעם הרביעית בערך) — כתובות hash מגיעות מלחיצה על Visit בדף דיפלוי ספציפי בדשבורד של ורסל. **הכלל: גולשים ומשתפים אך ורק את הכתובת הקנונית.**

### חקירת "רק אושר מצליח להזין אימונים" — אבחון מלא מול המסד החי (11/08)
אורי ביקש לאתר למה אף משתתף מלבדו ומלבד אושר בן שהם לא מזין אימונים. שלב א' — פורנזיקת קוד + git; שלב ב' — אימות מול המסד החי (אורי התחבר לדשבורד, השאילתות רצו ב-SQL Editor, קריאה בלבד). **הממצא: שתי בעיות נפרדות, ואף אחת מהן איננה באג סנכרון פעיל.**

**שכבה 1 — כולם היו חסומים שבוע שלם (4/8→10/8): פער הפצת התוכנית.** תוכנית מגיעה למשתמש קיים **רק** דרך הטמעה בקוד (applyProgram רץ כשה-PROGRAM_VERSION בבאנדל גדול מה-pv שבבלוב); shared_program נקרא רק בזריעת משתמש טרי (boot.js:1729). ההטמעה פיגרה אחרי הלוח: ב-03/08 הוטמעו רק ראשון-שני של שבוע 4 (PV 15), וכל השאר — רק ב-10/08 בערב (PV 28, v1.8.0). מי שפתח את האפליקציה משלישי 4/8 עד 10/8 ראה בכל יום נוכחי את מסך **"COMING SOON — האימון של היום עוד לא פורסם"** בלי שום שדה הזנה (app.html). ההוכחה החותכת מהמסד: חותמות ה-`_dts` של אושר — רשם את ראשון-שני של שבוע 4 ב-04/08 (הימים היחידים שהיו קיימים), ואז **כלום עד 10/08 20:36-20:50** — ספרינט השלמה רטרואקטיבי של שלישי-שבת + ראשון-שני של שבוע 5, רבע שעה אחרי עליית 1.8.0. אורי לא הרגיש בחסימה כי לאדמין יש "+ הזן אימון עכשיו" ביום ריק. **נפתר מאליו עם 1.8.0** — אבל שבוע 6 (מתחיל ראשון 16/8) יחזור על אותה חסימה אם לא יוטמע מראש.

**שכבה 2 — שלושת האחרים (Guy levi, אורן עוזיאל, מאי עוזיאל) לא פתחו את האפליקציה מאז 26-27/07.** מ-auth.users: `last_sign_in_at` של שלושתם = יום ההרשמה (25-26/07); רענוני הטוקן (updated_at) נעצרו 25-27/07; אין להם שורת states בכלל (אף סנכרון מוצלח מעולם); ה-board קפוא על ערב ההרשמה — ואורן אף לא השלים boot אחד (אין לו שורת board בכלל). כלומר אין באג שחוסם אותם היום — הם לא מגיעים בכלל: נטישה אחרי חוויית השבוע הראשון (מאי היא המשתמשת שאיבדה את אימוניה הראשונים ב-27/07), או קיר כניסה (סיסמה שנשכחה — אין איפוס עצמי לאימיילים הסינתטיים), או PWA שמותקן מכתובת hash מתה (`crossfit-bat-yam-capacity-68z7tjsh7` כבר מחזירה DEPLOYMENT_NOT_FOUND).

**אושר הוא הבייסליין הבריא:** נרשם 25/07, סשן אחד שחי מאז (רענון טוקן יומי), מכשיר שמתעדכן לבד (pv=28 תוך ~45 דקות מהרילים של 1.8.0), 18 ימים חתומים. ככה זה אמור להיראות אצל כולם.

**טכניקת אבחון שנשמרת:** `auth.users.last_sign_in_at` (כניסות סיסמה) מול `updated_at` (רענוני טוקן ≈ "האפליקציה חיה") מבדיל בין "לא פותח בכלל" ל"פותח ונכשל" — בלי לגעת בכלום.

### המשך אותו ערב: "כולם רואים את הגרסה החדשה אבל הכל נמחק" — הלוגים אומרים אחרת (11/08, 17:00)
אורי דיווח: כולם רואים את התוכנית והגרסה החדשה, מזינים — ובכל יציאה מה-PWA או רענון האימונים נמחקים; רק אושר שורד. נבדקו **לוגי ה-API המלאים של 24 השעות האחרונות** (Log Explorer, edge_logs):
- **בכל 24 השעות יש בדיוק שלושה מכשירים מול השרת:** האנדרואיד של אושר (115 דחיפות states + 116 board — הכול 200), המחשב של אורי, והאייפון של אורי. **אף מכשיר אחר. אפס כניסות סיסמה (חוץ מאחת של אורי), אפס הרשמות, ואפס תגובות שגיאה (אין אף 4xx/5xx).**
- מסקנה: הדיווחים "מזינים ונמחק" לא מתרחשים מול השרת הנוכחי — מי שמדווח לא נכנס לאפליקציה בכלל בימים האחרונים (אין שום עקבה). כנראה חוויה ישנה משבוע ה-COMING SOON או מעידן 1.6.x שמסופרת כהווה, ו/או אייקון PWA מת מכתובת דיפלוי מחוקה, ו/או סיסמה שנשכחה (עוצר בדלת, בלי ניסיון שמגיע לשרת).
- על הסטאק הנוכחי (1.8.0) התסמין המתואר לא יכול לקרות בלי להשאיר עקבות: משתמש עם סשן שדוחף — מופיע בלוגים; דחיפה שנכשלת — מופיעה כשגיאה; משתמש בלי סשן — כלל לא נכנס לאפליקציה (מוקפץ ללוגין).
- אגב הבדיקה: אורי מחק ב-14:03 את אורן עוזיאל ומאי עוזיאל (rpc אחד מהפאנל + כנראה אחד מהדשבורד) — נשארו 3 חשבונות: אורי, אושר, Guy levi. הרשמה מחדש שלהם = זריעה נקייה מ-shared_program, אמורה לעבוד כמו אצל אושר.
- **הצעד הבא שסוכם: בדיקה חיה מפוקחת** — מתאמן אחד שנכשל פותח את הכתובת הקנונית עכשיו, מתחבר (איפוס סיסמה מהפאנל אם צריך), מזין אימון בזמן שצופים בלוגים; כל מצב כשל יתגלה מיידית לפי הפוטר (☁️ מסונכרן / 🔑 התחברות פגה) והלוג.

### פיילוט הזנה חדשה — משקל לכל סט + תוצאה לכל תנועה (13/08)
אורי ביקש לשנות את אופן ההזנה — לנסות על אימון אחד, ואם טוב להטמיע על השאר; **בלי לגעת במספר הגרסה** עד הפריסה המלאה. הפיילוט הוטמע על **האימון של 6.8** (שבוע 4, חמישי — נבחר כי ה-LIFT שלו הוא בדיוק המקרה שאורי תיאר: Back Squat ‏6 סטים עם אחוזים מטפסים 83→88%, ושני השינויים נבדקים על יום אחד):
- **LIFT — משקל לכל סט:** במקום שדה משקל יחיד — שישה שדות SET 1-6 (המשקלים משתנים מסט לסט גם כשכתוב להינעל על משקל אחד). **הסט הכבד ביותר משתקף אוטומטית ל-`log.weight`** — טבלת השיאים, גרף ההתקדמות, סיכומי הלוח ושער השמירה ממשיכים לקרוא בדיוק את אותו שדה כמו תמיד. מתחת: שורת "🏆 הסט הכבד ביותר — Xkg — נרשם לטבלת השיאים" + שדות "חזרות בסט הכבד" והערה.
- **METCON — תוצאה לכל תנועה ואז ללוח:** שלב 1 — שדה לכל אחת משלוש התנועות (Cal row / Double DB hang snatches / Box jumps, `log.mv1..mv3`); שדה ה-Score ללוח **נעול 🔒 עד שכל השלוש מולאו**, ואז נפתח עם רמז "ממוצע התנועות: X" (הציון של האימון הוא ממוצע חזרות לדקה). METCON B של אותו יום נשאר בממשק הרגיל.
- **איך זה בנוי:** שתי מפות קונפיג בקומפוננטה (`PER_SET_LIFT` / `PER_MOVE_METCON`, מפתח `'שבועIdx_יוםIdx'` → `'3_4'`) שנקראות בזמן רנדור. אין שינוי בתוכנית המוטמעת, אין הקפצת PROGRAM_VERSION (נשאר 28) ואין שינוי גרסה (BUILD/version.json נשארים 1.8.0) — הפיילוט מגיע לכולם עם הבאנדל עצמו. פריסה מלאה בהמשך = הוספת מפתחות למפות (או פירסור אוטומטי של "X sets") + הקפצת הגרסה הגדולה שאורי הבטיח.
- **בטיחות סנכרון (הלקח מ-1.7.x):** ערכי סטים/תנועות ריקים **נמחקים** מה-log ולא נשמרים כ-'' — אומת מול `cleanLog`/`logSig` האמיתיים של boot.js שיום שנוקה זהה בחתימתו ליום ריק (אין חותמת-שווא שתנצח מיזוג ימים), ושערכים אמיתיים כן נכנסים לחתימה ונדחפים. `normalizeDay` לומד לשמר `s\d+`/`mv\d+` — בלעדיו השדות החדשים היו נמחקים בכל פתיחת אפליקציה.
- **אומת ב-dev (localhost + ‎?dev=1):** ‏6 שדות סטים מרונדרים על 6.8 בלבד; הזנת 100→117.5 מחשבת מקס נכון כולל מחיקת סט (117.5→115 וחזרה); הנעילה נשארת אחרי 2/3 תנועות ונפתחת על 3/3 עם ממוצע 12; שמירת יום מלאה עובדת; השיא "Back Squat ‏117.5kg × 2" מופיע בטאב שיאים; round-trip של normalizeDay על הקוד המוטמע (8 בדיקות) עובר; היום הנוכחי (13/8) ומטקון B מציגים את הממשק הישן ללא שינוי; מובייל 375px — הסטים בגריד 3×2 בלי גלילה אופקית; אפס שגיאות קונסול.

---

## SQL שרץ על המסד (Supabase → SQL Editor)
- סכימה + מדיניות אדמין + `is_admin()` ✅
- עמודות `gender` + `birth_date` בפרופילים ✅
- realtime על טבלת הלוח ✅
- עמודת `metcons` בלוח (מנוע הניקוד) ✅ (הורץ 23/07)
- פונקציית מחיקת משתמש (`admin_delete_user`) ✅ (אומת מול המסד החי 27/07 — קיימת ודוחה לא-אדמינים)
- פונקציית איפוס סיסמה (`admin_set_password`) ✅ (אומת מול המסד החי 27/07)
- שלוש עמודות v1.6.0 — `board.pub`, `shared_program.announcement`, `profiles.announcement_seen` ✅ (אומת מול המסד החי 27/07)
- פונקציית `push_days` (ערוץ הסגירה של v1.7.3) ✅ (אורי הריץ 01/08; אומת מבחוץ — קיימת, חסומה לאנונימיים)

## פתוח / הבא בתור
- 🔜 **אורי בודק את פיילוט ההזנה על האימון של 6.8** (שבוע 4 → חמישי): משקל לכל סט ב-LIFT + תוצאה לכל תנועה במטקון לפני פתיחת ה-Score. אם טוב — פריסה לכל האימונים (הוספת מפתחות ל-`PER_SET_LIFT`/`PER_MOVE_METCON` או פירסור אוטומטי של מספר הסטים מהטקסט) + **דחיפת הגרסה המסיבית שאורי הבטיח**. עד אז — שום שינוי גרסה.
- 🔴 **להחזיר את שלושת הנוטשים (מחקירת 11/08):** Guy levi, אורן עוזיאל, מאי עוזיאל לא נכנסו מאז 26-27/07. הודעה אישית עם הכתובת הקנונית + הצעה לאפס להם סיסמה מהפאנל (👥 משתתפים → איפוס סיסמה) אם שכחו; מי שמותקן לו PWA מכתובת ישנה — למחוק את האייקון ולהתקין מחדש מהכתובת הקנונית. את כל מה שפספסו אפשר להשלים רטרואקטיבית.
- 🔴 **להטמיע את שבוע 6 לפני ראשון 16/8** — אחרת חוזרת חסימת ה-COMING SOON של 4-10/08 (תוכנית מגיעה למשתמשים קיימים רק דרך הטמעה בקוד). כלל קבוע מעכשיו: כל שבוע מוטמע לפני שהוא מתחיל.
- 💡 שיפור מבני שהוצע בחקירת 11/08 (טרם אושר): שמשתמשים קיימים ימשכו תוכן תוכנית מ-shared_program בכל פתיחה (מה שאורי כותב באפליקציה יגיע לכולם בלי דיפלוי, וההטמעות בקוד יהפכו לגיבוי בלבד). דורש זהירות מול חוקי הסנכרון + egress.
- 🔴 **גיבוי ראשון:** פאנל משתתפים → "💾 גיבוי לקובץ", ולשמור את הקובץ בדרייב/מחשב. מומלץ קבוע: פעם בשבוע.
- 🧹 למחוק מפאנל "משתתפים" את שני משתמשי הבדיקה: **test-claude-delete-me** (מאבחון 25/07) ו-**test-claude-sync2** (מאבחון 27/07). המחיקה עובדת — `admin_delete_user` קיימת במסד.
- 🔜 מעקב חודשי קצר: Supabase → Usage → לוודא ש-egress מתחת ל-5GB. מתקרבים? שוקלים Pro (25$, כולל גיבוי יומי).
- ✅ ~~לאשר את מספר הגרסה 1.7.1 ולפרוס~~ — 1.7.1, 1.7.2 ו-1.7.3 פרוסות. **עדיין פתוח:** להזין מחדש את אימון שישי שבוע 2 מהסרטון (45 ק"ג · 5+4 RX · RPE 5) — הוא לא נשמר בשום מקום. עדיף אחרי הרצת ה-SQL של push_days.
- 🔜 אורי מוודא בטלפון (על 1.7.4): (א) בתחתית כתוב "גרסה 1.7.4" ולידו שורת מצב הענן; (ב) רושם אימון → בפוטר מופיע "☁️ שומר בענן…" ואז "☁️ מסונכרן לענן · שעה"; (ג) סגירה מיידית אחרי רישום → פתיחה → האימון נשאר; (ד) המחשב הפתוח מתעדכן לבד תוך ~שניות מהחזרה אליו. אם בפוטר מופיע "🔑 ההתחברות פגה — הקישו כאן" — הקשה אחת ולהתחבר מחדש; זה יסביר את כל מה שנראה כ"לא נשמר".
- ✅ ~~הטמעת שבוע 4~~ — **הושלם 10/08** (ראשון-שני ב-03/08, שלישי-שבת ב-10/08, PROGRAM_VERSION ‏15→20). ✅ ~~החלטת גרסה~~ — **אורי בחר 1.8.0 ונפרס (10/08)**.
- ✅ ~~הטמעת שבוע 5~~ — **הושלם 10/08** (כל השבוע הוטמע מראש באותו יום, כולל אינדורנס לראשון; PROGRAM_VERSION ‏20→28). הבא: אימוני שבוע 6 (יתחיל 16/8) — יידרש `programWeek6()` + שורת `overlay(5, ...)`. שבוע 5 (התחיל 9/8) עדיין לא קיים בקוד — יידרש `programWeek5()` + שורת overlay. **בלי לשנות מספר גרסה** (הנחיית אורי, אושרה שוב 10/08) — רק PROGRAM_VERSION; בסוף אורי בוחר את המספר.
- ✅ ~~**תיקונים מומלצים מהביקורת (27/07):**~~ **בוצעו ב-v1.7.2 (28/07)** — למעט תיקון ה-RPC לערוץ הסגירה (ראה למטה). למקור: (1) להפסיק את ההשתקה ב-`keepalivePush` — לוג על תשובה לא-תקינה, לא לנקות את חותמת ה-dirty בכשל, ולדלג על גוף >60KB לטובת המסלול הרגיל; (2) לבדוק `r.error` בשבעת מקומות ה-upsert שמתעלמים מהתוצאה (החמור: פרסום `shared_program` של האדמין, ו-`index.html` בהרשמה); (3) לנסח מחדש את הטוסט ב-`app.html` כך שיטען "נשמר במכשיר" בלבד — ההבטחה על הענן שייכת רק לחיווי של boot.js; (4) חיווי "ההתחברות פגה — התחבר מחדש" **בלי** הפניה אוטומטית ובלי חסימת כתיבה; (5) `doPushBoard` עדיין קורא מהאחסון ולא מ-`memTracker` כמו `doPushState`. **אף אחד מאלה לא מוסיף egress.**
- 💡 ~~מיזוג ברמת הימים~~ — **נבנה ב-v1.7.0 (27/07).** ~~רעיון המשך: RPC שמקבל רק ימים שהשתנו~~ — **נבנה ב-v1.7.3 (30/07)**; ממתין רק להרצת ה-SQL. רעיון המשך עתידי: להעביר גם את הדחיפה השוטפת (debounce/heartbeat) ל-RPC הזה ולהוריד ~70KB egress מכל שמירה — נגיעה במסלול הראשי, לעשות בזהירות ורק אם ה-egress יתקרב לתקרה.
- 💡 רעיונות שדוברו ולא נבנו: טיימר אימון מובנה (AMRAP/EMOM/ForTime), מסך ניתוח למאמן (נוכחות/נשירה), גרף מגמת RPE שבועי, אייקוני סוג אימון ברצועת THIS WEEK, צביעת מפת התוכנית לפי RPE, תקציר שבועי לשיתוף (html2canvas כבר בפרויקט). ~~הרחבת מערך התגים~~ — נבנה ב-v1.6.0 (15 תגים).
