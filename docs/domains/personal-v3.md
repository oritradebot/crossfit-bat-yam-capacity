# 🎯 תחום: v3 — אפליקציה אישית, בלי דירוג

**מקור:** אפיון עם אורי, 07/09/2026 (יום 1 של תחזוקת מעבר הבלוק), ארבעה סבבי שאלות בחירה, 16 הכרעות.
**סטטוס: שלב 1 (הסרה) נדחף 07/09 ערב (8432b0e); שלב 2 (סנכרון) נדחף 07/09 לילה (8fcb3d6) — ממתין לבדיקת אורי עם משתמש בדיקה; שלב 3 (יומן) נדחף 07/09 לילה (8374004); שלב 4 (סקירה) נדחף 07/09 לילה (67817aa); שלב 5 (שיאים ומבחנים) נדחף 07/09 לילה (53e14e2); שלב 6 (פאנל) נבנה 07/09 לילה, אומת מקומית ככל שאפשר בלי ענן; שלב 7 (מילים) טרם.** **גרסת יעד: 3.0.0** — אורי: *"אנחנו הולכים לשנות הרבה דברים, בסוף נשיק את גרסא 3.0.0"*.
כל ההכרעות כאן הן של אורי אלא אם כתוב "החלטה טכנית שלי".

---

## 1. העיקרון

> *"הבנתי שהחשיבות של כל מתאמן להתחרות בעצמו היא החשובה"* — אורי, 07/09.

- כל מה שמשווה מתאמן לאחרים **יורד**: מקומות, אחוזונים, קטגוריות, לוח, כרטיס של מתאמן אחר.
- במקום: **עובדות על עצמו והפרשים מול עצמו** — אימונים, ק"ג, זמן, חזרות, מבחן בסיס מול רי-טסט.
- **בלי ציונים מומצאים.** שום נוסחה, שום "ציון שבועי", שום "עקביות 80%".
- **פרטיות מלאה** (לעכשיו): מתאמן רואה רק את עצמו. ההשוואה בין מתאמנים נשארת אצל המאמן בלבד, בפאנל.

---

## 2. הכרעות אורי — הטבלה המלאה

| # | שאלה | הכרעה |
|---|---|---|
| 1 | הטאב 🏅 דירוג | **מוחקים לגמרי.** נשארים עם שלושה טאבים: סקירה, יומן, שיאים |
| 2 | כרטיס מתאמן אחר (תגים ושיאים של חבר) | **מוחקים** |
| 3 | מין + תאריך לידה בכניסה הראשונה | **ממשיכים לשאול, בלי קטגוריה.** גיל (ומין) נשארים בפרופיל לשימוש עתידי |
| 4 | כפתורי RX / Scaled | **נשארים כסימון אישי.** הכיתוב בלי "נכנס לדירוג" |
| 5 | השם המוצג (היום נערך בטאב הדירוג) | **עובר לכרטיס הפרופיל** |
| 6 | הכרטיס שמתפנה בסקירה | **"מטקוני RX בבלוק"** |
| 7 | אותות התקדמות אישית | **מבחני Capacity באפליקציה** · **"בפעם הקודמת עשית…"** · **המטרה האישית מוצגת** (עם "הושג"). סיכום שבועי אישי — *לא* נבחר |
| 8 | ציון מספרי על עצמו | **בלי ציונים מומצאים** |
| 9 | היסטוריה בין בלוקים | **בלוק 2 דף חלק.** הארכיון בשרת + הגיבויים נשארים אצל אורי |
| 10 | מבחנים בבלוק 2 | **עדיין לא ידוע** → מסך מבחנים גנרי, שיבוץ כשהתוכן יגיע |
| 11 | "ביחד" בלי תחרות | **פרטיות מלאה** (לעכשיו) |
| 12 | תצוגת מאמן | **כן, טבלה בפאנל** — נוכחות, מבחנים, שיאים; בלי מקומות |
| 13 | איפה מסך המבחנים גר | **בתוך טאב השיאים** → "שיאים ומבחנים" |
| 14 | גרסה | **3.0.0 בסוף** |
| 15 | תזמון הדחיפה | **בנפרד, בזמן התחזוקה**, הדגל נשאר דולק. תוכנית בלוק 2 נדחפת אחר כך על בסיס נקי |
| 16 | ההסבר למתאמנים (עזרה + הודעת בלוק 2) | **טיוטה שלי לאישור אורי.** שום דבר לא נשלח בלי אישורו |

---

## 3. מצאי ההסרה — כל מקום שהדירוג נוגע בו

### `public/app.html`
| מה | איפה (סימן לחיפוש, לא מספר שורה) | פעולה |
|---|---|---|
| כפתור הטאב | `🏅 דירוג` / `setBoard` | מחיקה |
| בלוק הטאב כולו | `<sc-if value="{{ isBoard }}">` … עד סוף הבלוק (כרטיס הניקוד, גרף 8 שבועות, כפתורי קטגוריה, שדה השם, טבלת העונה, הסבר "איך מחשבים") | מחיקה |
| כרטיס "המקום שלי בקטגוריה" בסקירה | `statRank` / `rankDeltaLabel` | מוחלף בכרטיס RX (סעיף 4.1) |
| פרופיל: "מיקום בקטגוריה" + תגית הקטגוריה | `profileCatRank` / `profileCategory` | מחיקה. מין + גיל נשארים |
| כרטיס מתאמן אחר | `<sc-if value="{{ peerOpen }}">` + `peerId` / `onBoardRow` / `peerStale` | מחיקה |
| מנוע הדירוג | `computeWeek`, `weekMetconsFromData`, `rankThrough`, `trend`, `boardRows`, `myPlaceText`, `MAXW` | מחיקה |
| עורך האדמין "ניהול מתחרים" | `boardEditorEl`, `editAthleteName`, `editAthleteWeek`, `addAthlete`, `removeAthlete` | מחיקה |
| קטגוריות | `catLabel`, `catPillStyle`, רשימת הקטגוריות, `selCategory`, `myCategory` | מחיקה |
| state | `board`, `selBoardWeek`, `boardMode`, `peerId`, `editingName`/`nameDraft` (עוברים לפרופיל) | ניקוי |
| כיתוב כפתור RX | `'עשיתי RX — נכנס לדירוג'` + ה-title `'…נכנס לדירוג'` | שינוי נוסח (סעיף 4.7) |
| מסך העזרה | שורות `📊 סקירה` ("המקום שלך בקטגוריה"), `🏅 דירוג`, `👤 פרופיל` ("הקטגוריה") | שכתוב |
| טקסט "השם שיופיע בטבלת הדירוג" בשאלון ההיכרות | `ONB` | שכתוב (השאלון עצמו מושבת ממילא, ראה 4.3) |
| הערות קוד "board score" בימי PER_MOVE_SCORE | `'תוצאה ללוח'` בכיתובי ההזנה | שינוי נוסח ל"תוצאה" |
| seed ל-dev | `devMain` בבוט + `BOARD_KEY` | עדכון |

### `public/assets/js/boot.js`
| מה | פעולה |
|---|---|
| בניית הלוח מכל הרוסטר ב-boot (`fetchAllProfiles` + `fetchBoard` + מיפוי לכל מתאמן) | **יורד.** ב-boot נשלפת רק השורה שלי (שם) |
| מנוי realtime על טבלת הלוח (`subscribeBoard` / `refreshBoard` / `cfbyReloadBoard`) | **יורד.** אין לוח משותף, אין מה לרענן |
| `extractMetcons` + דחיפת עמודת `metcons` | **יורד.** העמודה נשארת במסד, לא נכתבת יותר |
| `categoryOf` / `window.cfbyCategoryOf` / `myCategory` ב-seed | **יורד** |
| `publicSummary` + עמודת `pub` | **נשאר ומורחב** — הופך ל"שורת הסיכום למאמן" (סעיף 4.6) |
| `pushBoard` (שם + `weeks` + `pub`) | **נשאר** — זה מה שמזין את טבלת המאמן |
| שאלון מין + תאריך לידה (`askProfileDetails`) | **נשאר כמו שהוא** (הכרעה 3) |

### מסד הנתונים
| מה | פעולה |
|---|---|
| טבלת `board` | **נשארת**, משנה תפקיד: שורת סיכום אישית שרק בעליה והאדמין קוראים (SQL בסעיף 7) |
| `board.metcons` | נשארת ריקה/ישנה, לא נקראת ולא נכתבת |
| `profiles.gender` / `birth_date` | נשארות ונאספות |
| `profiles.goal` + `goal_done_at` | **חדשות** (SQL בסעיף 7) |

---

## 4. מה נבנה

### 4.1 כרטיס "מטקוני RX בבלוק" (סקירה)
- מספר גדול: כמה מטקונים סומנו RX **ויש להם תוצאה** — אותו כלל בדיוק כמו `rxCount` בכרטיס הסיכום (מטקון ראשי + מטקון שני, בלי אינדורנס), כדי שהמספר יהיה זהה בשני המקומות.
- שורה קטנה: "מתוך N מטקונים שתועדו". לחיצה → טאב היומן.

### 4.2 השם המוצג בכרטיס הפרופיל
- שדה עריכה + "שמור" בכרטיס הפרופיל, במקום השדה שהיה בטאב הדירוג.
- **החלטה טכנית שלי:** שינוי שם מעדכן גם את `profiles.name` (השורה שלי, מותר ב-RLS) ולא רק את `board.name` — כדי שהפאנל, הפרופיל והסיכום יציגו שם אחד. היום הפאנל מציג `profiles.name` והאפליקציה `board.name`, ויכולים להתפצל.

### 4.3 המטרה האישית
- **גילוי מהאפיון:** שאלון ההיכרות של האפליקציה (שם, יעד שבועי, מטרה) **מושבת מאז ההתחלה** על ידי שכבת הטעינה (`cfby_onb_v1` מסומן לפני ה-boot). לכן המטרה ויעד הנוכחות **מעולם לא נאספו ממשתמש אמיתי**; היעד תמיד 5 והמטרה תמיד ריקה. זה לא "להחזיר" תכונה — זה לבנות אותה.
- **איפה מזינים:** בכרטיס המטרה עצמו בסקירה. מצב ריק: "מה המטרה שלך לבלוק?" + שדה + שמור. מצב מלא: הטקסט + כפתור "✓ הושג" (הפיכה) + עיפרון לעריכה.
- **איפה נשמר — החלטה טכנית שלי:** `profiles.goal` (טקסט) + `profiles.goal_done_at` (חותמת או ריק). לא בבלוב ה-tracker: בלוב ה-tracker ממוזג יום-יום ב-boot, ושדה עליון היה דורש כלל מיזוג משלו. בפרופיל זה שדה אחד, שליפה שכבר קיימת, ו-upsert של השורה שלי. השליפה בבוט חייבת לסבול עמודה חסרה (אותה תבנית fallback כמו `announcement_seen`).
- **בלוק 2 דף חלק גם כאן:** באיפוס בלוק המטרה נמחקת? **לא** — הפרופיל לא נמחק באיפוס (רק states + board), והמטרה היא של האדם, לא של הבלוק. אם אורי ירצה מטרה-לבלוק, זה כפתור "מטרה חדשה" ולא איפוס.

### 4.4 "בפעם הקודמת עשית…"
- ביום אימון, מתחת ל-LIFT ומתחת לכל מטקון: אם **באותו בלוק** היה יום קודם עם אותו שם (מנורמל), מוצגת שורה: "בפעם הקודמת · שבוע N יום X: 80 ק"ג" / "12:30" / "5+12", ואם אותו `resultMode` — גם ההפרש עם צבע.
- **נרמול שם:** אותיות קטנות, חיתוך אחרי " — " (כמו טבלת השיאים), הסרת `(RETEST)` / `(Baseline)` / `CapaciTest #N`, רווחים. כך "Back Squat — CapaciTest #1 (RETEST)" פוגש את "Back Squat — CapaciTest #1 (Baseline)".
- אין קודם → לא מוצג כלום. לא מציגים "אין נתון".
- **בבלוק 1 החזרות האמיתיות היו רק המבחנים** (שבוע 1 מול 8); רוב המטקונים הופיעו פעם אחת. תרגילי הכוח חוזרים הרבה — שם זה יעבוד מהיום הראשון.
- **לא חוצה בלוקים** (הכרעה 9). הגרף לכל תרגיל בטאב השיאים כבר מראה את כל ההיסטוריה של הבלוק — זה משלים אותו, לא מחליף.

### 4.5 מסך המבחנים — בטאב השיאים
- הטאב נקרא **"שיאים ומבחנים"**. חלק עליון: `🧪 מבחני Capacity`. מתחתיו השיאים והגרפים כמו היום, ללא שינוי.
- שורה לכל מבחן: שם קצר, **בסיס** (ערך + שבוע), **רי-טסט** (ערך + שבוע, או "עדיין לא נמדד"), **הפרש** (צ'יפ צבעוני לפי `betterWhen`). זו אותה שורה שכרטיס הסיכום מצייר, בגרסת מסך.
- **מקור אמת יחיד:** רשימת המבחנים והשיבוצים היא `CAPACITESTS` שבתוך ה-IIFE של כרטיס הסיכום (חשוף כ-`BlockRecap.tests`). המסך קורא **את אותו קונפיג** ואת אותם קוראי-ערך (`readSlot`, `buildTest`). לא מכפילים. `buildTest` עדיין לא חשוף מה-IIFE — חושפים אותו **קודם ב-`design/block-recap/recap-build.js` ואז מדביקים מחדש** (כלל מקור-האמת של הכרטיס, ראה [[block-recap-card]]).
- **קונפיג ריק = החלק מוסתר לגמרי.** בלוק 2 עוד לא ידוע (הכרעה 10): עד שאורי ישלח, הרשימה מתרוקנת ממבחני בלוק 1, והחלק לא מוצג. כשהתוכן יגיע — `/embed-week` ממלא בסיס + רי-טסט, כמו שנעשה בשבוע 8.
- אין דירוג בין מבחנים, אין "ציון מבחנים". רק לפני / אחרי / הפרש.

### 4.6 טבלת המאמן בפאנל
- חלק חדש בפאנל: `📊 התקדמות המתאמנים`. עמודות: שם · אימונים בבלוק · שבועות מלאים · רצף הכי ארוך · שיאים · מטקוני RX · מבחנים (בסיס → רי-טסט לכל מבחן) · עדכון אחרון. **מיון לפי שם.** אין מקום, אין ניקוד, אין צבע "טוב/רע" בין אנשים.
- **מקור הנתונים — החלטה טכנית שלי:** שורות `board` (שם, `weeks` עם `completed` לכל שבוע, `pub` עם המונים) + `profiles`. לא בלובי ה-states: 100 מתאמנים × 70KB = 7MB לכל פתיחת פאנל, וזה ה-egress שאנחנו שומרים עליו. שורת סיכום היא 1-2KB.
- `pub` מורחב ב-`tests: [{ key, base, after }]`. הקורא בבוט (על JSON של ה-tracker, לא על מופע האפליקציה) משקף את `readSlot` — אותם מקורות: `lift.weight` / `lift.reps` / `metcon` / `metcon2` / `extra0` / `extra1`. הקונפיג נלקח מ-`window.BlockRecap.tests` (האפליקציה כבר טעונה בזמן הדחיפה).
- הכלים שרצים על גיבוי (`tools/block_summary.py`) ממשיכים לעבוד — הם קוראים את הגיבוי, לא את הפאנל.

### 4.7 טקסטים
- כפתור RX: `'עשיתי כמו שכתוב (RX)'`, title: `'ביצוע במשקלים ובתנועות שכתובים באימון'`. Scaled ללא שינוי.
- מסך העזרה: שורת הסקירה בלי "המקום שלך בקטגוריה"; שורת הדירוג נמחקת; שורת השיאים → "שיאים ומבחנים — שיאי המשקל לפי תרגיל, המבחנים שלך לפני/אחרי, ואימונים שסימנת 🏆"; שורת הפרופיל בלי "הקטגוריה".
- כיתובי "תוצאה ללוח" בימי PER_MOVE → "תוצאה".
- **טיוטת הודעת בלוק 2** (הגישה החדשה, בקולו של אורי) — נכתבת בשלב 4 ומחכה לאישור. לא מתפרסמת מהקוד.

---

## 5. מה נשאר בכוונה ללא שינוי

- **כרטיס הסיכום** — אין בו דירוג (נוכחות, מבחנים, שיאים, תגים, שמור/שפר). לא נוגעים, חוץ מחשיפת `buildTest`.
- **היומן, טבלת השיאים, גרפי ההתקדמות, RPE, 15 התגים, הגייט וההודעות** — ללא שינוי.
- **מסלול הסנכרון של `states`** — לא נוגעים. חוקי הברזל 1-6 לא מושפעים. רק דחיפת הלוח מצטמצמת (בלי `metcons`).
- **חוק ברזל 7** (לא משנים `resultMode` בשבוע סגור) — **נשאר**, הנימוק משתנה: במקום "מערבב ערכים בדירוג" → "שובר את ההשוואה מול עצמך" (מבחנים + "בפעם הקודמת").

---

## 6. סדר בנייה מוצע — כל שלב = דחיפה ל-main עם הדגל דולק

> **עודכן בהמשך היום:** הסדר המלא, כולל חלק ב', נמצא ב-§ב.10. הטבלה כאן נשארת כתיעוד של שלב האפיון הראשון.

מסך התחזוקה בודק **רק** את הדגל, לא את מספר הגרסה, אז דחיפות ביניים לא מגיעות לאף מתאמן. אורי רואה אותן במצב מנהל/צוות.

| שלב | מה | דורש מאורי |
|---|---|---|
| **1. הסרה** | כל סעיף 3 + כרטיס RX (4.1) + שם בפרופיל (4.2) + טקסטים (4.7) + `BUILD`/`version.json` = **3.0.0**, `maintenance: true` | — |
| **2. מול עצמך** | מסך מבחנים (4.5) + "בפעם הקודמת" (4.4) + מטרה אישית (4.3) | SQL עמודות המטרה |
| **3. מאמן** | טבלת הפאנל (4.6) + `tests` ב-`pub` | SQL פרטיות `board_read` |
| **4. מילים** | טיוטת עזרה + הודעת בלוק 2 | אישור הנוסח |

אחרי שלב 4 ממשיך הצ'קליסט של [`block-transition.md`](block-transition.md): הטמעת תוכנית בלוק 2, `startDate`, שיבוצי מבחנים, איפוס, בדיקה, כיבוי הדגל.

**בדיקה מקומית לכל שלב:** `app.html?dev=1` (בלי Supabase) + השוואת `#cfbyVer` מול `BUILD` (מלכודת ה-SW). הרצת חילוץ ה-Component לפי סימן מחרוזת (השיטה מ-03/09) לאימות שהמבחנים והקוראים מחזירים ערכים.

---

## 7. SQL לאורי — פעם אחת, Supabase → SQL Editor

```sql
-- v3 שלב 2: המטרה האישית (נשמרת על האדם, לא על הבלוק — האיפוס לא נוגע בפרופילים)
alter table public.profiles add column if not exists goal         text;
alter table public.profiles add column if not exists goal_done_at timestamptz;

-- v3 שלב 3: פרטיות — שורת הסיכום נקראת רק על ידי בעליה או אדמין
-- (היום: כל מחובר קורא את כל השורות — זה שירת את הלוח המשותף שיורד)
drop policy if exists board_read on public.board;
create policy board_read on public.board for select to authenticated
  using (user_id = auth.uid() or public.is_admin());
```

סדר: ה-ALTER לפני דחיפת שלב 2, שינוי המדיניות **אחרי** דחיפת שלב 1 (קוד ישן במכשיר צוות שעדיין מושך את כל הלוח יקבל שגיאה שקטה ויציג את העותק האחרון — לא מזיק, אבל אין סיבה). `schema.sql` מתעדכן ידנית באותו commit ([[schema-drift]]).

---

## 8. ❌ מה נדחה ולמה

| הרעיון | למה נדחה | מתי |
|---|---|---|
| סיכום שבועי אישי בסוף כל שבוע | אורי לא בחר אותו מבין אותות ההתקדמות | 07/09 |
| טאב "הבוקס" / שאוט-אאוט על שיאים / ספירה קבוצתית | "פרטיות מלאה, מומלץ לעכשיו". אפשר להוסיף אחרי בלוק 2 | 07/09 |
| היסטוריה של בלוק 1 באפליקציה | "בלוק 2 דף חלק". הארכיון בשרת + הגיבויים + כרטיסי ה-PNG מספיקים | 07/09 |
| אחוז מהיעד / ציון עקביות | "בלי ציונים מומצאים" | 07/09 |
| טאב רביעי "מבחנים" | בלוק בלי מבחנים היה משאיר טאב ריק; נכנס לתוך השיאים | 07/09 |
| לשמור realtime "ליתר ביטחון" | אין לוח משותף — אין למי לרענן. חיסכון egress | 07/09 (טכני) |
| לשמור את המטרה בבלוב ה-tracker | דורש כלל מיזוג לשדה עליון; בפרופיל זה upsert אחד | 07/09 (טכני) |
| טבלת מאמן מבלובי ה-states | 7MB לפתיחת פאנל ב-100 מתאמנים | 07/09 (טכני) |
| יעד נוכחות אישי (3-6 בשבוע) | לא עלה באפיון. נשאר 5 קבוע כמו שהיה בפועל | 07/09 |

---

**קשור:** [`block-transition.md`](block-transition.md) (הצ'קליסט שממשיך אחרי שלב 4) · [`program.md`](program.md) (שיבוצי מבחנים דרך `/embed-week`) · [`backup.md`](backup.md) (הכלים על גיבוי לא מושפעים)

---
---

# חלק ב' — שמונה תחומי שיפור נוספים ל-3.0.0

**מקור:** המשך אותו אפיון, 07/09/2026 אחר הצהריים. אורי בחר לאפיין עוד לפני שנוגעים בקוד; מתוך מפת התחומים בחר שמונה. שישה סבבי שאלות בחירה, 24 הכרעות. כמו בחלק א': הכרעה = אורי, "החלטה טכנית שלי" = שלי.

## ב.1 הזנת האימון ביומן

| הכרעה | פירוט |
|---|---|
| **כלל ההשלמה נשאר קפדני** | יום נספר רק כשכל מה שבתוכנית תועד (משקל לכוח + תוצאה לכל מטקון). **ההודעה "חסר: …" הופכת משורת טוסט שנעלמת לשורה קבועה ליד הכפתור**, עם פירוט לפי חלק (למשל "חסר: תוצאה למטקון"). זה מה שתקע את אורן ודוד ב-04/09 — הם לא ראו את הטוסט. |
| **תיבת משקל לכל סט נשארת** + כפתור **"אותו משקל לכולם"** | ממלא את כל הסטים במשקל אחד, ואז מתקנים רק את החריגים. `PER_SET_LIFT` ללא שינוי. |
| **סרגל המאמץ: 3 רמות במקום RPE 1-10** | קל / בסדר / קשה. ראה הערה טכנית. |
| **תוצאה לכל תנועה — ברירת מחדל לכל מטקון עם ≥2 תנועות, קפדני** | היום רק בימי פיילוט (`PER_MOVE_METCON`). התוצאה הכוללת נפתחת רק כשכל התנועות מולאו — כמו הפיילוט, בכל מקום. |

**הערה טכנית — 3 רמות (החלטה שלי):**
- ערכים: `day.rating` ∈ `'easy' | 'ok' | 'hard'` (וגם `alt.rating`). אימוג'י: 😌 💪 🥵.
- `normalizeDay` ממיר היום סולם ישן `easy/good/hard/max` ל-RPE מספרי (`FEEL2RPE`). **המרה זו נמחקת** — בלוק 2 דף חלק, אין נתונים ישנים שצריך להמיר, והמפתחות `easy`/`hard` חוזרים לשימוש. שארית RPE מספרית (אם תגיע מאיפשהו) מוצגת בלבד: 1-4 → קל, 5-7 → בסדר, 8-10 → קשה.
- תג `🥵 5 אימוני RPE 9+` → `🥵 5 אימונים קשים` (`r9` → `hard` בסיכום `pub` ובכרטיס הסיכום — שלושת המקומות חייבים להישאר זהים: `badgeList`, `publicSummary`, recap).
- טקסט השיתוף וכרטיס היום מציגים את המילה, לא מספר.

**הכרעת אורי (07/09, סוף היום): קפדני בכל מקום.** הצעתי שתיבות התנועה יהיו רשות ורק התוצאה הכוללת תידרש להשלמה, כדי שמטקון של 4 תנועות לא יהפוך לחמישה שדות חובה — **נדחה.** הסמנטיקה של הפיילוט חלה על כל מטקון עם ≥2 תנועות: תיבה לכל תנועה, והתוצאה הכוללת נפתחת רק כשכולן מלאות; בלי כולן — היום לא נספר, וההודעה הקבועה ליד הכפתור אומרת בדיוק איזו תנועה חסרה. `PER_MOVE_METCON` הופך למפת **חריגים** (ימים שבהם *לא* רוצים תיבות לתנועות), `PER_MOVE_BOXES` נשאר לרשימות מותאמות, `PER_MOVE_SCORE` נשאר לימים שבהם התוצאה משקפת תיבה.

## ב.2 מסך הסקירה

| הכרעה | פירוט |
|---|---|
| **הכרטיסים:** אימונים שהושלמו X/40 · יעד השבוע X/5 · שבוע נוכחי X/8 · **מטקוני RX בבלוק** (חלק א') · **המטרה** (חלק א') | "אחוז מהיעד" **יורד** (כפילות). כרטיס "האימון הראשון שלך" (ב.4) מופיע מעל הכול עד התיעוד הראשון. |
| **רצועת THIS WEEK: אייקון סוג אימון לכל יום** | 🧪 מבחן (השם מכיל CapaciTest) › 🚴 אינדורנס (יש `alt`) › 🏋️ כוח (יש lift עם תוכן) › ⏱️ מטקון בלבד › ➕ תוספת בלבד · 🛌 מנוחה · ריק = COMING SOON. הכותרת עוברת ל"השבוע". |
| **מפת התוכנית — ללא שינוי** | הושלם / לא סומן / מנוחה / היום. הכותרת ל"מפת התוכנית". |
| **יעד שבועי 5 קבוע לכולם** | לא נבנית בחירה. `WEEKLY_TARGET` נשאר, `myTarget` נשאר 5. |

## ב.3 תצוגת תוכנית האימון

| הכרעה | פירוט |
|---|---|
| **בלוקים מתקפלים** | כל בלוק (LIFT / METCON / METCON B / EXTRA / סיכום) נפתח בלחיצה על הכותרת. בלוק שכבר יש בו תוצאה מתקפל לשורה אחת עם התוצאה; הבלוק הבא שחסר נפתח. מצב הקיפול בזיכרון בלבד, לא נשמר. |
| **שורות הרמה → טבלה** | שורות בתבנית `L1 = …` / `Level 2 = …` / `L3-4 — …` מזוהות בזמן רינדור ומוצגות כטבלה קטנה (רמה · תוכן). שאר הטקסט נשאר כמו שהוא. **תוכן התוכנית לא משתנה** (חוק ברזל 6) — זה רק תצוגה. |
| **שפה: מונחים מקצועיים באנגלית, כותרות מסך בעברית** | נשארים: LIFT, METCON, EXTRA, RX, Scaled, AMRAP, EMOM, שמות תרגילים. עוברים לעברית: THIS WEEK → השבוע, PROGRAM MAP → מפת התוכנית, SESSION SUMMARY → סיכום האימון, PERSONAL RECORDS → שיאים ומבחנים, Movement/Planned/Your Result → תרגיל / תוכנית ודגשים / התוצאה שלי. טקסט מעורב: `dir="auto"` + `unicode-bidi: plaintext` על כל פסקת תוכנית, כדי שהשורה תיפתח לפי התו הראשון שלה. |
| **התוכנית נשארת בקוד לבלוק 2** | `/embed-week` כמו היום. "מהענן בלי דיפלוי" נדחה לבלוק 3 לכל המוקדם. |
| **רמה לכל אימון, נשמרת עם התוצאה** | פילים L1 · L2 · L3 · L4 ליד RX/Scaled (L4 קיים בחלק מהאימונים). נשמר `day.level` / `alt.level`. ברירת המחדל: הרמה האחרונה שנבחרה (נגזרת מסריקת הימים הקודמים — בלי אחסון חדש). "בפעם הקודמת עשית…" (חלק א' 4.4) מציג את הרמה של הפעם הקודמת ומסמן אם שונה. לא חובה להשלמה. |
| **כרטיסי שיתוף יום/שבוע — נשארים** | ללא שינוי. |

## ב.4 קליטת מתאמנים חדשים

| הכרעה | פירוט |
|---|---|
| **הרשמה עצמית** היא הדרך | כמו 04/09. יצירה מהפאנל נשארת למקרי קצה. |
| **מדריך 3 מסכים בפתיחה הראשונה** | (1) מה יש כאן — שלושה טאבים; (2) איך מתעדים — בחר יום, רשום, שמור והשלם; (3) התקנה על הטלפון. פעם אחת, אפשר לדלג. השער: `profiles.welcome_seen` (קיים; היום שכבת הטעינה משתיקה את המדריך של האפליקציה — במקומו בא זה, אחרי שאלת המין/תאריך הלידה). |
| **כרטיס "האימון הראשון שלך"** בראש הסקירה | שלושה צעדים לסימון (פתח את האימון של היום · רשום תוצאה · שמור והשלם). נעלם אחרי היום הראשון שהושלם. |
| הודעות ההצטרפות (טיוטות 04/09) | **נכתבות מחדש** ל-v3: בלי דירוג, בלי קטגוריה, "שיאים ומבחנים". שלב "מילים". |

## ב.5 תזכורות ומעורבות

| הכרעה | פירוט |
|---|---|
| **בלי התראות לטלפון, בלי נדנוד בתוך האפליקציה** | הכול דרך הפאנל + וואטסאפ של אורי. |
| **הפאנל מסמן:** לא תיעד **14 יום** · הזנה חלקית שלא הושלמה · שבוע מבחנים מתקרב | "14 יום" — אורי קבע (הצעתי 3 ימים). "יעד שבועי בסכנה" לא נבחר. |
| **טקסט וואטסאפ מוכן להעתקה** | כפתור 📋 ליד כל שורה מסומנת מעתיק הודעה אישית (שם, מה חסר בדיוק, הקישור הקנוני). שלוש תבניות: לא-פעיל / חלקי / מבחנים. אורי מדביק ושולח. |
| **חגיגה קטנה על שיא ורצף** | קונפטי (קנבס, בלי ספרייה, כשנייה וחצי) + כרטיס "שיא חדש 🏆 Back Squat 100 ק"ג" עם כפתור שיתוף (פותח את כרטיס היום). טריגרים: סימון 🏆 / משקל שעובר את השיא הקודם לתרגיל / רצף 5 ו-10 ימים (ספי התגים). רק למתאמן עצמו. |

**החלטה טכנית שלי — מקור הנתונים לסימונים:** `board.pub` מורחב ב-`last_done` (תאריך היום המושלם האחרון), `partial` (מספר + רשימת `w_d` של ימים עם הזנה בלי השלמה) ו-`tests` (חלק א'). מחושב במכשיר בזמן הדחיפה, מהבלוב המקומי. הפאנל קורא רק שורות `board` — לא בלובי `states`. מי שלא פותח את האפליקציה בכלל: `pub` שלו מתיישן וזה בדיוק "לא פעיל". "שבוע מבחנים מתקרב" = השבוע הבא מכיל שיבוץ `retest`; הרשימה: מי שיש לו `base`.

## ב.6 הפאנל

| הכרעה | פירוט |
|---|---|
| **טבלת המשתתפים: עמודת מצב** | 🔴 לא פעיל (14 יום) · 🟠 הזנה חלקית (N ימים) · 🟢 פעיל. מיון לפי שם נשאר. 📋 העתק הודעה על 🔴/🟠. כניסה אחרונה / מכשיר / הערה חופשית — **לא** נבחרו. |
| **טבלת ההתקדמות** | חלק א' 4.6, ללא שינוי. |
| **מצב עריכה באפליקציה — מוסתר, לשעת חירום** | כפתורי צפייה/עריכה, הבונה וספריית התנועות לא מוצגים. הקוד נשאר. מופעל רק ע"י מתג "🛠 מצב עריכה" בפאנל (דגל מקומי במכשיר). |
| **תזכורת גיבוי** | פס בולט בפאנל כשעברו 7 ימים מ-`cfby_backup_last` (או שאין). |

## ב.7 עיצוב ומראה

| הכרעה | פירוט |
|---|---|
| **המראה נשאר, ליטושים בלבד** | נייבי + כתום, Oswald למספרים, בהיר/כהה/אוטומטי. מלטשים רק מה שהשינויים נוגעים בו: הכרטיסים החדשים, הקיפול, טבלת הרמות, הכותרות בעברית. |

## ב.8 ביצועים וגודל

| הכרעה | פירוט |
|---|---|
| **השמירה השוטפת עוברת ל-`push_days`** — עכשיו, בתחזוקה | היום: כל שמירה = כל הבלוב (~70KB) דרך upsert; הערוץ של הימים (~2KB) רק בסגירה. ראה תוכנית טכנית. |
| **גיבויים: תזכורת בפאנל אחרי 7 ימים** | בלי Supabase Pro. |

**תוכנית טכנית — שמירה שוטפת דרך `push_days` (החלטה שלי, בגבולות ההכרעה):**
- `doPushState` שולח **רק** את הימים ב-`memDirtyDays` (הסט כבר קיים, משמש את ערוץ הסגירה) דרך `sb.rpc('push_days', { p_days, p_dts })`; ה-RPC כבר עושה last-writer-wins לפי חותמות יום, אותן חותמות של המיזוג בפתיחה.
- מנקים מהסט רק את הימים שנשלחו **ואושרו**; כישלון משאיר אותם (וחותמת ה-dirty נשארת — חוק ברזל 2 לא נוגע).
- **נפילה לבלוב המלא** (המסלול של היום) כש: אין מפתחות יום מלוכלכים אבל יש שינוי (עריכת תוכנית של אדמין, שינוי שאינו יום), RPC מחזיר 404, או יותר מ-20 ימים מלוכלכים (השלמה רטרואקטיבית מסיבית — הבלוב זול יותר).
- הדחיפה בפתיחה (התאוששות מחותמת dirty) נשארת בלוב מלא. המיזוג בפתיחה ללא שינוי. **חוקי ברזל 1-5 לא נוגעים.**
- אומדן: היום ~1MB לסשן הזנה (15 דחיפות × 70KB); אחרי — ~30KB. ב-100 מתאמנים: ~2GB/חודש → ~60MB.
- **בדיקה חובה עם משתמש הבדיקה של אורי** (מצב צוות, בזמן התחזוקה): הזנת יום → שורת `states` בענן מכילה את היום + חותמת; שני מכשירים; אופליין → אונליין; סגירה באמצע הקלדה.
- בונוס טכני: `html2canvas` (~200KB) נטען רק בלחיצה הראשונה על שיתוף, לא בפתיחה.

## ב.9 ❌ מה נדחה בחלק ב' — ולמה

| הרעיון | למה | מתי |
|---|---|---|
| השלמה חלקית נספרת / שני כפתורים שמור-השלם | "נשאר קפדני, הודעה בולטת" | 07/09 |
| רק הסט הכבד במקום תיבה לכל סט | נשאר לכל סט + מילוי מהיר | 07/09 |
| מילוי מהפעם הקודמת · הזנה מהירה מהסקירה | לא נבחרו | 07/09 |
| כרטיס "אחוז מהיעד" | כפילות של X/40 | 07/09 |
| תמצית תוצאה ברצועת השבוע | רק אייקון | 07/09 |
| מפה מקופלת / צבועה לפי מאמץ | נשארת כמו שהיא | 07/09 |
| יעד אישי 3-6 בפרופיל / לפי מאמן | 5 קבוע | 07/09 |
| תוכנית מהענן בלי דיפלוי | "נשאר בקוד לבלוק 2". לבלוק 3 לכל המוקדם | 07/09 |
| רמה קבועה בפרופיל | במקום זה רמה לכל אימון | 07/09 |
| יצירת חשבונות מהפאנל כברירת מחדל | הרשמה עצמית | 07/09 |
| נדנוד בתוך האפליקציה · התראות אמיתיות (web push) | פאנל + וואטסאפ. push = שרת + מפתחות + אישורים + אייפון מותקן בלבד — לא לבלוק 2 | 07/09 |
| טריגר "3 ימים בלי תיעוד" · "יעד בסכנה" | אורי: 14 יום; היעד לא נבחר | 07/09 |
| כניסה אחרונה (דורש RPC) · מכשיר וגרסה · הערה חופשית בפאנל | לא נבחרו | 07/09 |
| הודעה אישית בתוך האפליקציה | טקסט להעתקה במקום | 07/09 |
| מחיקת מצב העריכה מהקוד | מוסתר, לא מחוק | 07/09 |
| רענון עיצובי ל-3.0.0 | ליטושים בלבד | 07/09 |
| Supabase Pro | תזכורת גיבוי מספיקה; לשקול רק אם התעבורה תתקרב לתקרה | 07/09 |

## ב.10 סדר הבנייה המעודכן — מחליף את §6

כל שלב = דחיפה ל-main עם `maintenance: true`. `BUILD`/`version.json` = **3.0.0** משלב 1 (המתאמנים לא רואים מספר גרסה מאחורי הדגל; אורי רואה במצב צוות). אורי בודק בטלפון אחרי שלבים 2, 3 ו-4.

| שלב | מה | דורש מאורי |
|---|---|---|
| **1. הסרה** ✅ 07/09 | חלק א' §3 כולו + כרטיס RX (4.1) + שם בפרופיל (4.2) + טקסטים בסיסיים (4.7) + 3.0.0. אומת ב-`app.html?dev=1`: טעינה נקייה, 3 טאבים, כרטיס RX סופר, עריכת שם נשמרת, כיתוב RX חדש, מסך עזרה בלי דירוג | — |
| **2. סנכרון** ✅ נבנה 07/09 לילה | `push_days` בשמירה השוטפת (ב.8) + טעינה עצלה של html2canvas. אומת מקומית: boot.js נטען, html2canvas נמשך רק בלחיצת שיתוף. **מסלול ה-RPC עצמו לא ניתן לבדיקה בלי ענן** | **משתמש בדיקה** במצב צוות (הזנת יום → השורה בענן מתעדכנת; שני מכשירים; אופליין→אונליין; סגירה באמצע) + SQL פרטיות `board_read` (כבר ב-schema.sql) |
| **3. יומן** ✅ נבנה 07/09 לילה | "חסר" קבוע (תיבה כתומה + צ'יפים בכותרות הבלוקים) · "אותו משקל לכולם" · 3 רמות מאמץ (`easy`/`ok`/`hard`; תג "5 אימונים קשים"; `pub.hard`) · תוצאה לכל תנועה כברירת מחדל, קפדני (`PER_MOVE_OFF` לחריגים) · רמה לכל אימון (`day.level`, ב-logSig) · קיפול בלוקים · טבלת רמות (`planEl`) · כותרות ושמות ימים בעברית + `unicode-bidi:plaintext` · "בפעם הקודמת" (כוח לפי תרגיל, מטקון לפי שם מלא בלי תגי מבחן). אומת ב-dev: הזנה מלאה מקצה לקצה, קיפול, הפרשים | — |
| **4. סקירה** ✅ נבנה 07/09 לילה | כרטיסים X/40 · X/5 · X/8 · RX (כרטיס האחוזים ירד) · כרטיס המטרה (הזנה במקום, "הושג?", נשמר ב-`profiles.goal`/`goal_done_at` דרך דחיפת הלוח; בלי העמודות נשאר במכשיר) · אייקוני סוג אימון ברצועת השבוע (`dayIcon`) · כרטיס "האימון הראשון שלך" עד היום המושלם הראשון · חגיגה (קונפטי על `<html>` + כרטיס עם שיתוף) על 🏆, על משקל שעובר את שיא הבלוק לתרגיל (מושהה 1.4 שנ' אחרי ההקלדה, פעם ליום), על רצף 5/10 ועל מטרה שהושגה. אומת ב-dev | **SQL עמודות המטרה** (§7; כבר ב-schema.sql) |
| **5. שיאים ומבחנים** ✅ נבנה 07/09 לילה | הטאב נקרא "שיאים ומבחנים"; חלק עליון "🧪 מבחני Capacity" עם כרטיס לכל מבחן (לפני / אחרי / צ'יפ הפרש, לחיצה פותחת את היום; "עדיין לא נמדד" / "אין מדידה חוזרת בתוכנית"; מוסתר כשהקונפיג ריק). `buildTest` נחשף ב-`design/block-recap/recap-build.js` והודבק מחדש מילה-במילה (אומת). אומת ב-dev: 100 ← 110 = "+10% · +10 ק\"ג" | — |
| **6. פאנל** ✅ נבנה 07/09 לילה | `pub` מורחב: `partial` + `partial_keys` (סשנים עם הזנה בלי השלמה), `last_gi`, `tests` (דרך `window.BlockRecap._readSlot` על shim של ה-tracker) · טבלת "📊 התקדמות המתאמנים" מ-`board` בלבד (אימונים, שבועות מלאים, רצף, שיאים, RX, קשים, מבחנים לפני→אחרי, עדכון; מיון לפי שם) · עמודת מצב במשתתפים (🔴 14 יום לפי `states.updated_at` · 🟠 חלקי · 🟢 · ⚪) עם 📋 הודעת וואטסאפ אישית (3 תבניות) · באנר שבוע מבחנים (`window.cfbyStartDate` + שיבוצי retest) · באנר גיבוי אחרי 7 ימים · מתג "🛠 מצב עריכה" (דגל `cfby_editmode` במכשיר + אירוע לאפליקציה); כפתורי הבונה/תנועות/צפייה-עריכה מוסתרים בלעדיו. **הפאנל עצמו לא ניתן לבדיקה בלי ענן** — `publicSummary` אומתה בדף dev על tracker אמיתי, הסתרת הכלים אומתה | בדיקה במצב מנהל אחרי הדחיפה |
| **7. מילים** | מדריך 3 מסכים · מסך עזרה · הודעת בלוק 2 · הודעות הצטרפות מעודכנות | אישור נוסח |

ואז הצ'קליסט של [`block-transition.md`](block-transition.md): תוכנית בלוק 2, `startDate`, שיבוצי מבחנים, איפוס, בדיקה, כיבוי הדגל.

**SQL:** אין חדש מעבר לשניים שב-§7 (עמודות המטרה לפני שלב 4, מדיניות `board_read` אחרי שלב 1). `schema.sql` מתעדכן באותו commit.
