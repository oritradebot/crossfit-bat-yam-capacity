# 💾 גיבוי, ייצוא וסיכומים — מה אנחנו יודעים עכשיו

עודכן: 06/09/2026 (סוף בלוק 1, לפני האיפוס).

## איפה הגיבויים

- **המקור היחיד לנתוני כולם** הוא הכפתור "💾 גיבוי לקובץ" בפאנל האדמין (boot.js, `backup()`): ארבע טבלאות (`profiles`, `states`, `board`, `shared_program`) בקובץ JSON אחד, `format:"cfby-backup"`, `ver:1`. אין service key במחשב ואין CLI — הדרך לנתונים היא סשן אדמין בדפדפן.
- **הקובץ יורד לתיקיית ההורדות של כרום של אורי, שהיא לא "Downloads":** `C:\Users\leaan\Desktop\crossfit manager project\CFBY Capacity Dashboard\`. שם יושבים כל הגיבויים: 26/07, 10/08 (על שולחן העבודה, בשם אחר), 05/09 ("batyam-backup end of 1st block.json"), 06/09.
- הפאנל מופיע רק במצב המנהל של האפליקציה (`localStorage.cfby_admin === "1"`, לוגו + קוד). הדלקה מקונסול/JS של הדגל מספיקה כדי שהטאב "👥 משתתפים" יופיע; לכבות בסוף כדי להשאיר לאורי את התצוגה הרגילה.
- לחיצה על 💾 ממכשיר של אורי חותמת `cfby_backup_last` באותו מכשיר ("גיבוי אחרון: היום") — החותמת היא לכל מכשיר, לא לחשבון.

## הכלים (tools/) — עובדים על קובץ הגיבוי בלבד, לא נוגעים בענן

| כלי | מה עושה | הרצה |
|---|---|---|
| `tools/block_summary.py` | לכל משתתף: `סיכום.html` (נוכחות, מבחני CapaciTest ש1↔ש8, הישגים, שיאים, יומן מלא), `backup.json` (המשתתף בלבד, בפורמט שהפאנל משחזר), `recap-card.json`; ולכולם `index.html` + `all-sessions.csv` | `python tools/block_summary.py [backup.json] [out]` — בלי פרמטרים: הגיבוי החדש ביותר בתיקייה למעלה → `block1-summary-<תאריך הגיבוי>` לידו |
| `tools/recap_cards_server.py` + `tools/recap-harness.html` | מרנדר את **כרטיס הסיכום האמיתי** (recap-card.js + recap-build.js מ-`design/block-recap/`) לכל משתתף ושומר `recap-story.png` (1440×2560) + `recap-feed.png` (1440×1800) בתיקיית המשתתף | תצורת `recap-cards` ב-`.claude/launch.json` (או `python tools/recap_cards_server.py [backup] [out] [port]`), ואז לפתוח `http://localhost:8171/?auto=1` בדפדפן **עם אינטרנט** (Oswald + Heebo מגיע מגוגל; בלי הפונטים הצילום יוצא בפונט אחר). הסיכומים נגישים גם ב-`/out/index.html` |

החישובים בשני הכלים הם פורט 1:1 של האפליקציה (`dayComplete`, `metconScore`, `dayHasPR`, משבצות `CAPACITESTS`, שיא = המשקל הכבד ביותר לתרגיל, יעד שבועי 5). כל שינוי בלוגיקה של app.html/recap-build.js צריך להשתקף גם כאן — אחרת הסיכום יסתור את המסך.

## עובדות על הנתונים שהתגלו בייצוא (06/09)

- **`myTarget` לא מסונכרן לענן** (boot.js מזריע רק `myName/myResults/myCategory`), ולכן כל חישוב מהגיבוי משתמש ביעד 5. מתאמן שבחר יעד 4 בטלפון יראה בכרטיס שלו מספרים שונים במקצת מהגיבוי.
- **מבחן #3 (Strict Pull Up) לא הופיע לאף אחד:** הכרטיס קורא את המספר המוביל מטקסט חופשי של `extras[0].log.text` ביום שני של שבוע 1 — אף אחד לא כתב שם מספר. אם רוצים את המבחן בבלוק 2, צריך שדה מספרי ולא טקסט.
- **נתוני ניסיון מיולי חיים בשורות אמיתיות:** אצל אורי ואצל אושר יום ראשון של שבוע 1 מכיל בדיוק את אותו לוג (Back Squat 55 ק"ג × 1), ואצל אושר גם "זמן" 21:26 על AMRAP — הערך של המבחן האירובי של אורי. ה-"+100%" של אושר (55→110) נשען על זה. לא שונה — זה מה שהכרטיס באפליקציה מציג; לאורי להחליט.
- **Guy levi** בלי שורת `states` בכלל (רק `board` מ-27/07) — הכלים מציגים אותו כ"אין תוצאות בענן" ומפיקים לו כרטיס ריק.
- `states` של מתאמן "ריק" שוקל ~170KB (התוכנית מוטבעת בכל בלוב); של אושר ~184KB. הגיבוי המלא של 7 משתמשים: 830KB.
- שמות המשתתפים בכרטיס = `board.name` (מה שהמתאמן הקליד), עם נפילה ל-`profiles.name`.

## מה נדחה ולמה

- **לא תוקנו/נוקו נתונים** (הבסיס הכפול של 55 ק"ג, ה-21:26 על AMRAP): ייצוא הוא קריאה בלבד; שינוי בנתוני מתאמן הוא החלטה של אורי.
- **הכרטיסים לא נשלחו למתאמנים** — הופקו לתיקייה בלבד. המתאמנים עדיין יכולים להפיק לבד מהאפליקציה עד האיפוס.
- **גיבוי `auth.users`** (סיסמאות/חשבונות) לא כלול בגיבוי הפאנל ולא נגיש מהדפדפן; האיפוס לא נוגע בו, ולכן זה בסדר.
- **הפקת הכרטיסים דורשת דפדפן** (html2canvas + פונטים) — אין גרסת Python טהורה, ולא שווה לבנות אחת.

## הנוהל החוזר

`/block-export` (‎`.claude/skills/block-export/SKILL.md`) — גיבוי מהפאנל דרך הכרום של אורי → `tools/block_summary.py` → `recap-cards` → אימות → יומן. **תמיד להשתמש בו לפני איפוס/סוף בלוק, לא לאלתר.**

## הכרעות אורי (06/09)

- "אתה לא מתעסק בתחזוקה כרגע" — מצב תחזוקה/השבתה זמנית **נדחה**, לא מתחילים בלי בקשה מפורשת.
- כשהמחיקה של `states`+`board` תיבנה: **כפתור בפאנל עם אישור מוקלד** (בחירתו), לא SQL ידני.
- רעיון "אפוק בלוק" (השרת מסמן בלוק פעיל, מכשיר זורק עותק מקומי של בלוק ישן גם אם לא סונכרן) — **"לא רלוונטי"**. לא להציע שוב.

## עוד עובדות (הועבר מהזיכרון 10/09/2026)

- **שחזור** = upsert שורה-שורה. שורות של משתמש שנמחק בינתיים נכשלות על ה-FK ל-`auth.users` ו**מדולגות, לא עוצרות**; לעומת זאת `backup()` עוצר על כל שגיאת טבלה.
- גדלים: 209KB (26/07), 712KB (05/09), ואז חבילת 06/09 `batyam-backup-bundle-2026-09-06/` (+zip) עם ה-JSON, ‏`schema.sql` ו-`manifest.md` בעברית.
- **שולחן העבודה לא ב-OneDrive** — הגיבויים מקומיים בלבד עד שאורי מעתיק לדרייב.
- קנה מידה: 50→~100 משתמשים — Vercel סטטי לא בעיה, Supabase חינמי מספיק; **לעקוב אחרי egress (5GB/חודש)**. Pro (25$/חודש) קונה בעיקר גיבוי יומי.
- על המחשב הזה אין `pg_dump` / `psql` / supabase CLI / node / docker, וכולם היו דורשים את סיסמת המסד — לכן הגיבוי הוא רק דרך הפאנל.
