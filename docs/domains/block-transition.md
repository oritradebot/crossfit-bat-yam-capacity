# 🔄 תחום: מעבר בין בלוקים — מצב תחזוקה, איפוס וארכיון

**מקור:** אפיון עם אורי, 06/09/2026 (ערב סוף בלוק 1). **סטטוס: נבנה באותו לילה (v2.3.0), ה-SQL הורץ על המסד החי ע"י אורי באותו לילה (אומת אנונימית: הפונקציה והטבלה קיימות); נדחף ל-main.**
**גרסה מיועדת:** 2.3.0 (אורי בחר). כל ההחלטות כאן הן של אורי אלא אם כתוב אחרת.

---

## 1. המהלך כולו, בסדר

1. כרטיס הסיכום נפתח לכולם (06/09) → חלון שמירה.
2. דיפלוי **2.3.0**: מצב תחזוקה דולק + כפתור איפוס בפאנל + כלל השאריות. העלאת הגרסה היא מה שמושך אפליקציות פתוחות למסך התחזוקה.
3. ממתינים שהתעבורה תשקוט (לוגים של Supabase, `states.updated_at` לא זז).
4. פאנל: 💾 גיבוי טרי → 🧨 איפוס בלוק (ארכיון ואז מחיקה).
5. ימי התחזוקה: שיפור תשתית, הטמעת תוכנית בלוק 2, `startDate` חדש, שיבוצי retest חדשים.
6. בדיקה עם משתמש בדיקה מהפאנל (עובר את מסך התחזוקה דרך מצב צוות במכשיר של אורי).
7. 🧨 איפוס שני — מנקה את נתוני הבדיקה.
8. דיפלוי גרסת בלוק 2: הדגל כבוי + גרסה חדשה. דפי התחזוקה חוזרים לאפליקציה לבד.
9. אורי פותח ראשון (הדחיפה שלו מפרסמת את שלד התוכנית ל-`shared_program`), ואז הודעת בלוק מהפאנל.
10. 48 שעות של מעקב אחרי שורות `states` שחוזרות עם לוגים ישנים.

---

## 2. מצב תחזוקה

| רכיב | מה בדיוק |
|---|---|
| **הדגל** | `public/version.json` → `{ "build": "2.3.0", "maintenance": true }`. הדלקה/כיבוי = עריכת המילה ודחיפה. אורי יכול לבד דרך עורך הקבצים של GitHub (Vercel פורס את `main` תוך כדקה). **`build` חייב להישאר זהה ל-`BUILD` ב-boot.js.** |
| **השער** | `boot.js` → `main()`, **מיד אחרי** `getSession` + `fetchProfile` ו**לפני** `askProfileDetails`, שליפת ה-tracker וההתקנה של ה-interceptor. תנאי: `maintenance && !prof.is_admin && !staff` → `location.replace("maintenance.html")`. הדגל נשלף במקביל לפרופיל, ונכשל-פתוח אחרי 4 שניות. בנקודה הזאת אין `memTracker`, לא נכתב ולא נדחף כלום — חוק ברזל 3 לא רלוונטי (הוא על אמצע סשן). הנתונים המקומיים של המתאמן נשארים על המכשיר. |
| **דף ההתחברות** | `index.html` מקבל את אותה בדיקה (דגל + סימן צוות, בלי זהות). מנותק רואה תחזוקה, לא טופס. |
| **דף התחזוקה** | `public/maintenance.html`, סטטי, באותו עיצוב של דף ההתחברות (כרטיס על הגרדיאנט הכחול). הלוגו האמיתי `assets/logo.png` עגול עם טבעת לבנה כמו בכותרת האפליקציה. טקסט (מילה במילה): **"תכף נחזור לבלוק חדש"** / **"המשתמש שלכם נשמר - הנתונים מתאפסים לגמרי"**. מרענן את `version.json` (no-store) כל 60 שניות; דגל כבוי → `/app`. |
| **מצב צוות** | נקישה על הלוגו בדף התחזוקה → טופס קוד (`<form>`, כך ש-Enter / מקש Go שולחים בכל פלטפורמה). הקוד = `adminCode` של האפליקציה (`batyam`, אותו קוד לפי אורי). נכון → `localStorage.cfby_staff = "1"` → `/app`. סימן ברמת מכשיר, לכן משתמש בדיקה שאינו admin עובר במכשיר של אורי. |
| **admin** | `prof.is_admin` עובר תמיד, בלי מחווה. שליפת פרופיל שנכשלה (`_err`) נחשבת לא-admin → מסך תחזוקה, רענון פותר, או מצב צוות. |
| **אפליקציות פתוחות** | `checkFreshBundle` רץ בכל חזרה לחזית ומרענן **רק דף נקי** → נוחת בשער → תחזוקה. דף עם רישום לא מסונכרן ממשיך לרוץ עד שיסנכרן, ואז מתרענן. מכוון — לא מרעננים מעל נתונים לא מסונכרנים. |
| **SW** | network-first, לכן הדגל תמיד טרי. ה-fallback מהמטמון רלוונטי רק אופליין, ושם ממילא אין סנכרון. |

---

## 3. כפתור איפוס בלוק (פאנל האדמין)

- **מיקום:** ליד 💾 גיבוי / ♻️ שחזור.
- **היקף** (אורי: "למחוק את כל הנתונים באפליקציה, בלי לגעת בתשתית"): כל שורות `states` + `board` **כולל של אורי**; `shared_program`: `weeks = null`, `block_recap = null`, `announcement = null`. **נשארים:** `auth.users`, `profiles` (שם/מין/תאריך לידה/דגלים), `announcement_log`, כל הטבלאות והפוליסיז.
- **נעילות** (הכפתור מושבת אלא אם שתיהן מתקיימות):
  1. מצב תחזוקה דולק — הפאנל קורא את `version.json` (no-store).
  2. גיבוי טרי **מהמכשיר הזה** בתוך 60 הדקות האחרונות — לפי `cfby_backup_last` (`BK_KEY`) שהפאנל כבר מתחזק.
- **אישור:** הכפתור מציג "יימחקו נתונים של N מתאמנים" (N = משתמשים עם שורת `states` או `board`) ואורי מקליד את N. מספר שגוי = לא קורה כלום.
- **ביצוע:** פונקציית שרת אחת, `admin_reset_block(p_label text)`, SECURITY DEFINER, בודקת `is_admin()`, **טרנזקציה אחת**:
  1. `insert into block_archive` — כל שורת `states` ו-`board` יחד עם `profiles.name` באותו רגע.
  2. `delete from states`, `delete from board`.
  3. `update shared_program set weeks=null, block_recap=null, announcement=null where id=1`.
  4. מחזירה `{ archived_states, archived_board, deleted_states, deleted_board }` והפאנל מציג את המספרים ומרענן.
- **חוזר על עצמו:** נלחץ פעמיים בבלוק — נתוני בלוק 1 (תווית `block-1`), ואז נתוני הבדיקה רגע לפני הפתיחה (תווית `block-2-tests`). תווית חופשית, ברירת מחדל לפי הבלוק.
- **בלי מחיקות מהלקוח.** הכל דרך ה-RPC כדי שארכיון ומחיקה יהיו אטומיים.
- **הגנות בלחיצה (נבנו מעבר לאפיון):** שתי הנעילות נבדקות שוב ברגע הלחיצה; הכפתור מסרב כשיש דחיפה באוויר או נתונים לא מסונכרנים (`pushBusy || unsynced()`), כי upsert שנוחת אחרי ה-delete היה יוצר מחדש את השורה של אורי עם נתוני בלוק 1; אחרי הצלחה `dropLocalCopy()` ואז reload.
- הארכיון שומר גם את שורת `shared_program` (kind `program`), כך שהתוכנית, ההודעה והגייט של הבלוק שנסגר נשמרים.

### טבלת הארכיון

```sql
create table if not exists public.block_archive (
  id          bigserial primary key,
  label       text not null,                       -- 'block-1', 'block-2-tests', ...
  kind        text not null check (kind in ('states','board','program')),
  user_id     uuid,                                -- null לשורת התוכנית; בלי FK: הארכיון שורד מחיקת משתמש
  name        text not null default '',            -- שם המתאמן ברגע הארכוב
  data        jsonb not null,                      -- ה-tracker המלא / שורת הלוח המלאה
  archived_at timestamptz not null default now()
);
-- RLS: קריאה/כתיבה ל-admin בלבד (is_admin()).
```

הכל כבר ב-`supabase/schema.sql` (הקובץ כולו ניתן להרצה חוזרת). **הורץ על המסד החי 06/09 בלילה (אורי).** אומת אנונימית: ה-RPC מחזיר 401 (קיים, אסור ל-anon) מול 404 לפונקציה שלא קיימת; הטבלה מחזירה 200. הפאנל עדיין מזהה פונקציה חסרה ומסביר, למקרה של מסד אחר.

---

## 4. שאריות במכשירים (כלל "השורה נעלמה")

**נבנה אחרת מהאפיון המקורי (מזהה בלוק), לטובה.** במקום לחתום כל tracker במספר בלוק, המכשיר זוכר האם הוא כבר *פגש* את השורה שלו בענן:

- `boot.js` שומר `cfby_srvrow_v1 = "1"` בכל boot שבו השרת החזיר שורה, ובכל דחיפה מלאה שהצליחה.
- ב-`main()`, בענף "השרת ענה ואין שורה": `rowGone = אין שורה && יש tracker מקומי && הסימן קיים && הפרופיל נקרא בהצלחה (לא _err ולא _missing)`.
  - `rowGone` → העותק המקומי **נזרק** דרך `dropLocalCopy()` (מראות בזיכרון, טיימרים של דחיפה, המפתחות של tracker/dts/dirty/board/הסימן), זריעה מחדש כמו משתמש חדש, ואם הייתה חותמת dirty מוצגת פעם אחת ההודעה: *"נמצאו רישומים מהבלוק הקודם שלא סונכרנו, הם לא נכללים בבלוק החדש"* (הפופאפ של ההודעות, בלי קונפטי, כפתור "הבנתי").
  - אין שורה ובלי סימן → משתמש חדש שהדחיפות הראשונות שלו נכשלו: שומרים מקומי ודוחפים, **בדיוק כמו היום**.
  - יש שורה → מיזוג רגיל, בלי שינוי. לכן לפני האיפוס ההתנהגות זהה להיום.
- למה לא מזהה בלוק: לא תלוי בשעון המכשיר, לא דורש חתימה על כל כתיבה של האפליקציה (ה-normalize של האפליקציה היה מוחק שדה זר), ומכסה גם מחיקה של משתמש בודד מהפאנל.
- למה נדרש פרופיל מאומת: תחת RLS קריאה אנונימית (סשן מת) מחזירה "ריק" בלי שגיאה. `fetchProfile` מסמן עכשיו `_missing` כשהשורה לא חזרה. בלי ההוכחה הזאת, סשן מת היה נראה כמו איפוס.
- אחרי איפוס מהפאנל, המכשיר של אורי עצמו זורק את העותק שלו לפני ה-reload (`dropLocalCopy()`), כדי שדחיפת סגירה לא תחזיר את בלוק 1 לענן.

> ⚠️ **סתירה לתיעוד מסשן מקביל:** ב-`docs/domains/backup.md` (הכרעות 06/09, סשן הגיבויים) רשום שרעיון "אפוק בלוק" (השרת מסמן בלוק פעיל, המכשיר זורק עותק מקומי ישן) הוא "לא רלוונטי". באותו ערב, בסשן האפיון הזה, אורי ענה לשאלה ישירה "מה קורה לרישומי בלוק 1 שלא סונכרנו" ובחר **"להשליך + הודעה חד-פעמית"**, והסתירה הוצגה לו במפורש לפני שאמר "תתחיל". הכלל כאן צד-לקוח בלבד, בלי סימון בשרת.

## 5. מה נדחה ולמה

| נדחה | למה |
|---|---|
| **מתג תחזוקה בפאנל** (דגל ב-DB + RPC שנקרא לפני התחברות) | אורי בחר דגל בקובץ: פשוט, בלי SQL, בלי תלות ב-Supabase לפני login, והכניסה/יציאה ממילא נופלות על דיפלוי. לשקול לבלוק 3. |
| **גדר בשרת** על כתיבות `states` מבלוק ישן (טריגר שדוחה blob בלי `block` נוכחי) | מכשיר עם קוד ישן שנטען אופליין יכול ליצור מחדש שורה אחרי המחיקה. אבל כתיבה שנדחית משאירה דף בקוד ישן dirty לנצח, והוא אף פעם לא מתעדכן לבד (`checkFreshBundle` מרענן רק דף נקי). במקום: הודעה למתאמנים, מעקב 48 שעות, הרצה חוזרת של האיפוס. |
| **היסטוריית PR בין בלוקים** | הארכיון עכשיו מאפשר את זה; צריך UI והחלטה של אורי בבלוק 2. |
| **שחזור מארכיון בלחיצה** | ♻️ שחזור מ-JSON כבר מכסה rollback. |

---

## 6. צ'קליסט יום המחיקה

0. ✅ אורי הריץ את `supabase/schema.sql` המעודכן ב-SQL Editor (06/09 לילה; `block_archive` + `admin_reset_block` אומתו מול המסד החי).
1. דיפלוי 2.3.0 (דגל דולק). בטלפון של אורי: מסך תחזוקה כמנותק, מצב צוות עובד, admin עובר, `#cfbyVer` = 2.3.0.
2. Log Explorer: אין כתיבות ל-`states`/`board`; `select max(updated_at) from states` לא זז.
3. פאנל: 💾 גיבוי (חייב להיות בתוך השעה).
4. 🧨 איפוס: להקליד N → לוודא במספרים המוחזרים ובדשבורד: `states`/`board` ריקות, `block_archive` = N שורות מכל סוג.
5. לבנות בלוק 2 → בדיקות עם משתמש בדיקה → 🧨 איפוס שני.
6. דיפלוי בלוק 2 (דגל כבוי, גרסה חדשה) → אורי פותח ראשון → הודעת בלוק.
